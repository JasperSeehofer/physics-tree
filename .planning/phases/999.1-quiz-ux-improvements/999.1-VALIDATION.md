---
phase: 999.1
slug: quiz-ux-improvements
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-03-24
---

# Phase 999.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | cargo test (Rust) + wasm-pack test (WASM) |
| **Config file** | `Cargo.toml` workspace config |
| **Quick run command** | `cargo test -p db -p domain -- --quiet` |
| **Full suite command** | `cargo test --workspace -- --quiet` |
| **Estimated runtime** | ~30 seconds |

---

## Sampling Rate

- **After every task commit:** Run `cargo test -p db -p domain -- --quiet`
- **After every plan wave:** Run `cargo test --workspace -- --quiet`
- **Before `/gsd:verify-work`:** Full suite must be green
- **Max feedback latency:** 30 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| TBD | TBD | TBD | TBD | TBD | TBD | TBD | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

*Note: Task IDs will be populated after PLAN.md creation.*

---

## Wave 0 Requirements

*Existing infrastructure covers all phase requirements.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Green fill on correct MC answer | D-01 | Visual CSS styling | Select correct MC answer, verify green bg-leaf-green background |
| Checkmark icon on correct option | D-03 | Visual element | Select correct answer, verify checkmark icon appears |
| LaTeX renders in quiz text | D-06 | Browser rendering | Load quiz with $...$ in question text, verify KaTeX output |
| Formula preview renders fractions | D-08/D-09 | Browser + WASM | Type `\frac{1}{2}` in formula input, verify fraction renders |
| XP toast shows hint penalty text | D-12 | Visual text | Use hint then answer correctly, verify toast message |

*Visual verifications require browser testing.*

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 30s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
