---
status: diagnosed
phase: 999.1-quiz-ux-improvements
source: [999.1-01-SUMMARY.md, 999.1-02-SUMMARY.md, 999.1-03-SUMMARY.md]
started: 2026-03-25T18:30:00Z
updated: 2026-03-25T18:45:00Z
---

## Current Test

[testing complete]

## Tests

### 1. Green feedback on correct MC answer
expected: Select correct MC option → click "Check answer" → correct option turns solid green with checkmark, wrong options dimmed, "Correct!" text appears
result: pass

### 2. Hint shown on wrong MC answer
expected: Select wrong MC option → click "Check answer" → amber hint text appears ("Not quite — ..."). Select correct answer and click "Check answer" again → green feedback appears.
result: pass

### 3. Matching quiz green feedback
expected: On a matching-type quiz question, match all pairs correctly → click "Check" → all matched pairs turn solid green with checkmarks.
result: pass

### 4. LaTeX renders in quiz question text
expected: Quiz questions containing math (like $x(t) = 3t^2$) render as formatted math (superscripts, fractions, symbols) — not raw dollar-sign text.
result: issue
reported: "there are no dollar signs, but t^2 renders as t2 and fracs are not working either"
severity: major

### 5. Formula input preview
expected: On a formula-type quiz question, type \frac{1}{2} into the formula input. The live preview below should render a proper fraction (½ stacked) — not show raw braces or garbled text.
result: issue
reported: "it displays as 21"
severity: major

### 6. Hint penalty XP toast
expected: Get a question wrong (see hint), then answer correctly. The XP toast notification should show reduced XP amount and display "hint penalty applied" in teal text.
result: issue
reported: "cant reproduce, if I answer wrong and then correctly it just shows correct!"
severity: major

## Summary

total: 6
passed: 3
issues: 3
pending: 0
skipped: 0
blocked: 0

## Gaps

- truth: "Quiz questions with $x(t) = 3t^2$ render as formatted math with superscripts and fractions"
  status: failed
  reason: "User reported: there are no dollar signs, but t^2 renders as t2 and fracs are not working either"
  severity: major
  test: 4
  root_cause: "Effect calls renderAllPlaceholders() synchronously during reactive update, before DOM has committed inner_html content. Also KaTeX CSS may not be loaded (same underlying issue as test 5)."
  artifacts: [crates/app/src/components/quiz/multiple_choice.rs, crates/app/src/components/quiz/formula_input.rs, crates/app/src/js/katex_bridge.js]
  missing: [requestAnimationFrame/queueMicrotask deferral in Effect, KaTeX CSS verification]
  debug_session: .planning/debug/latex-plain-text-quiz.md

- truth: "Formula input preview renders \\frac{1}{2} as a proper stacked fraction"
  status: failed
  reason: "User reported: it displays as 21"
  severity: major
  test: 5
  root_cause: "KaTeX CSS (katex.min.css) not loaded in browser. katex_bundle.js imports CSS-in-JS but bundler may not extract/inject the stylesheet. KaTeX HTML structure is correct but renders unstyled — fraction num/den appear as flat adjacent text."
  artifacts: [crates/app/src/js/katex_bridge.js, katex_bundle build config]
  missing: [KaTeX CSS stylesheet in page, bundler CSS extraction config]
  debug_session: .planning/debug/formula-preview-21.md

- truth: "XP toast shows hint penalty applied in teal text when answering correctly after using a hint"
  status: failed
  reason: "User reported: cant reproduce, if I answer wrong and then correctly it just shows correct!"
  severity: major
  test: 6
  root_cause: "Two issues: (1) POST /api/progress/award-xp requires auth — returns 401 when not logged in, client silently swallows. (2) XP Effect only fires when ALL checkpoints answered, not per-question."
  artifacts: [crates/app/src/pages/concept.rs, crates/server/src/handlers/progress.rs]
  missing: [Visual feedback when not logged in, partial-completion XP consideration]
  debug_session: .planning/debug/xp-toast-never-appears.md
