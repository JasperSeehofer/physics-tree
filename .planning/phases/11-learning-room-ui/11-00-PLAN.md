---
phase: 11-learning-room-ui
plan: 00
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/server/tests/learning_room_integration.rs
  - crates/db/src/phase_progress_repo.rs
  - crates/app/src/components/content/markdown_renderer.rs
autonomous: true
requirements: [UI-01, UI-02, UI-03, UI-04, UI-05]

must_haves:
  truths:
    - "Test skeleton files exist with compilable stubs for all VALIDATION.md entries"
    - "Running cargo test compiles all test files (tests may be #[ignore]d but must compile)"
  artifacts:
    - path: "crates/server/tests/learning_room_integration.rs"
      provides: "Integration test stubs for Learning Room API endpoints"
      contains: "mod learning_room_integration"
    - path: "crates/db/src/phase_progress_repo.rs"
      provides: "Unit test stubs for phase progress CRUD (added by Plan 01, stubs placed here for Nyquist)"
    - path: "crates/app/src/components/content/markdown_renderer.rs"
      provides: "Extended test stubs for math events and GFM alerts (added by Plan 02, stubs placed here for Nyquist)"
  key_links: []
---

<objective>
Create test skeleton files referenced by VALIDATION.md so all plans have compilable Nyquist-compliant verification targets.

Purpose: VALIDATION.md declares 8 test commands that reference files/modules which do not yet exist. This Wave 0 plan creates those skeletons with `#[ignore]` stubs so that (a) verify commands compile from the start, and (b) implementation plans fill in real assertions.
Output: Compilable test stubs in 3 locations.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-learning-room-ui/11-CONTEXT.md
@.planning/phases/11-learning-room-ui/11-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create integration test skeleton for Learning Room API</name>
  <files>
    crates/server/tests/learning_room_integration.rs
  </files>
  <read_first>
    crates/server/tests/auth_integration.rs,
    crates/server/Cargo.toml
  </read_first>
  <action>
Create `crates/server/tests/learning_room_integration.rs` with `#[ignore]` test stubs matching VALIDATION.md entries 11-01-01, 11-01-05, and 11-01-06. Follow the pattern from `auth_integration.rs` for test structure.

```rust
//! Integration tests for Learning Room API endpoints.
//! Stubs created by Wave 0 — implementations filled by Plan 01 and Plan 05.

#[tokio::test]
#[ignore = "Wave 0 stub — implement in Plan 01"]
async fn test_get_learning_room_content_returns_phases() {
    // GET /api/learning-room/kinematics should return phases
    // VALIDATION ref: 11-01-01
    todo!("Implement: GET /api/learning-room/:slug returns phases for node with content")
}

#[tokio::test]
#[ignore = "Wave 0 stub — implement in Plan 01"]
async fn test_get_learning_room_content_404_for_unknown_slug() {
    // GET /api/learning-room/nonexistent should return 404
    // VALIDATION ref: 11-01-01
    todo!("Implement: GET /api/learning-room/:slug returns 404 for unknown slug")
}

#[tokio::test]
#[ignore = "Wave 0 stub — implement in Plan 01"]
async fn test_get_progress_empty_for_anonymous() {
    // GET /api/learning-room/kinematics/progress returns [] without auth
    // VALIDATION ref: 11-01-06
    todo!("Implement: anonymous progress returns empty array")
}

#[tokio::test]
#[ignore = "Wave 0 stub — implement in Plan 01"]
async fn test_post_progress_requires_auth() {
    // POST /api/learning-room/kinematics/progress returns 401 without auth
    // VALIDATION ref: 11-01-06
    todo!("Implement: POST progress requires authentication")
}

#[tokio::test]
#[ignore = "Wave 0 stub — implement in Plan 01"]
async fn test_post_progress_gate_enforced() {
    // POST phase 2 without phase 1 complete returns 403
    // VALIDATION ref: 11-01-06
    todo!("Implement: server-side gate blocks out-of-order phase completion")
}

#[tokio::test]
#[ignore = "Wave 0 stub — implement in Plan 05"]
async fn test_existing_content_endpoint_unchanged() {
    // GET /api/content/:slug still works (regression)
    // VALIDATION ref: 11-01-05
    todo!("Implement: existing content API unaffected by Learning Room changes")
}
```

The stubs use `#[ignore]` so `cargo test` compiles them but does not run them (they would panic on `todo!()`). Implementation plans remove `#[ignore]` and fill in assertions.
  </action>
  <verify>
    <automated>cargo test -p server --test learning_room_integration 2>&1 | grep -E "test result|running|ignored"</automated>
  </verify>
  <done>learning_room_integration.rs compiles with 6 ignored test stubs</done>
</task>

<task type="auto">
  <name>Task 2: Add unit test stubs for phase_progress_repo and markdown_renderer</name>
  <files>
    crates/db/src/phase_progress_repo.rs,
    crates/app/src/components/content/markdown_renderer.rs
  </files>
  <read_first>
    crates/db/src/phase_progress_repo.rs,
    crates/app/src/components/content/markdown_renderer.rs,
    crates/db/src/progress_repo.rs
  </read_first>
  <action>
NOTE: These source files do not exist yet (created by Plans 01 and 02). This task creates ONLY the files if they don't exist, or appends test modules if they do. Since Plan 00 runs in Wave 1 alongside Plans 01 and 02, the executor should check if files exist first.

**If `crates/db/src/phase_progress_repo.rs` does NOT exist yet**, create a minimal skeleton:

```rust
//! Phase progress repository — CRUD for user_phase_progress table.
//! Skeleton created by Wave 0 — implementation filled by Plan 01.

use sqlx::PgPool;
use uuid::Uuid;

// Stub struct — Plan 01 fills in real implementation
pub struct PhaseProgressRow {
    pub phase_number: i16,
    pub completed_at: chrono::DateTime<chrono::Utc>,
    pub format_pref: String,
}

pub async fn get_phase_progress(_pool: &PgPool, _user_id: Uuid, _node_id: Uuid) -> Result<Vec<PhaseProgressRow>, sqlx::Error> {
    todo!("Plan 01 implements")
}

pub async fn mark_phase_complete(_pool: &PgPool, _user_id: Uuid, _node_id: Uuid, _phase_number: i16, _format_pref: &str) -> Result<(), sqlx::Error> {
    todo!("Plan 01 implements")
}

#[cfg(test)]
mod tests {
    #[test]
    #[ignore = "Wave 0 stub — implement in Plan 01"]
    fn test_phase_progress_row_fields() {
        // VALIDATION ref: 11-01-04
        todo!("Verify PhaseProgressRow has correct fields")
    }

    #[test]
    #[ignore = "Wave 0 stub — implement in Plan 01"]
    fn test_mark_phase_complete_idempotent() {
        // VALIDATION ref: 11-01-04
        todo!("Verify ON CONFLICT DO NOTHING behavior")
    }
}
```

Also add `pub mod phase_progress_repo;` to `crates/db/src/lib.rs` if not already present.

**If `crates/app/src/components/content/markdown_renderer.rs` ALREADY exists**, append test stubs to the existing `#[cfg(test)] mod tests` block (or create one). Add these stubs:

```rust
#[cfg(test)]
mod tests {
    // ... existing tests ...

    #[test]
    #[ignore = "Wave 0 stub — implement in Plan 02"]
    fn test_math_events_inline() {
        // VALIDATION ref: 11-01-07
        // $E=mc^2$ -> <span data-latex="E=mc^2" data-display="false">
        todo!("Implement math event test")
    }

    #[test]
    #[ignore = "Wave 0 stub — implement in Plan 02"]
    fn test_math_events_display() {
        // VALIDATION ref: 11-01-07
        // $$F=ma$$ -> <div data-latex="F=ma" data-display="true">
        todo!("Implement display math event test")
    }

    #[test]
    #[ignore = "Wave 0 stub — implement in Plan 02"]
    fn test_gfm_alert_note() {
        // VALIDATION ref: 11-01-08
        // > [!NOTE]\n> text -> <div class="admonition admonition-note">
        todo!("Implement GFM alert test")
    }

    #[test]
    #[ignore = "Wave 0 stub — implement in Plan 02"]
    fn test_gfm_alert_warning() {
        // VALIDATION ref: 11-01-08
        todo!("Implement GFM warning alert test")
    }
}
```

If the file does not have a test module yet, create one. If it does, add the stubs inside the existing module.
  </action>
  <verify>
    <automated>cargo test -p db --lib -- phase_progress_repo::tests 2>&1 | grep -E "test result|running|ignored" && cargo test -p app --features ssr --lib -- markdown_renderer::tests 2>&1 | grep -E "test result|running|ignored"</automated>
  </verify>
  <done>phase_progress_repo has 2 ignored test stubs, markdown_renderer has 4 ignored test stubs. All compile.</done>
</task>

</tasks>

<verification>
- `cargo test -p server --test learning_room_integration` compiles (all tests ignored)
- `cargo test -p db --lib -- phase_progress_repo::tests` compiles (all tests ignored)
- `cargo test -p app --features ssr --lib -- markdown_renderer::tests` compiles (all tests ignored)
- `cargo build --workspace` succeeds
</verification>

<success_criteria>
- All VALIDATION.md test commands compile successfully
- Test stubs are `#[ignore]` so they don't block CI
- Implementation plans (01, 02) will fill in real assertions and remove `#[ignore]`
</success_criteria>

<output>
After completion, create `.planning/phases/11-learning-room-ui/11-00-SUMMARY.md`
</output>
