---
phase: 10-manual-pilot-node
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - content/classical-mechanics/kinematics/node.yaml
  - content/classical-mechanics/kinematics/phase-0.md
  - content/classical-mechanics/kinematics/phase-1.md
  - content/classical-mechanics/kinematics/phase-2.md
  - content/classical-mechanics/kinematics/phase-3.md
  - content/classical-mechanics/kinematics/phase-4.md
  - content/classical-mechanics/kinematics/phase-5.md
  - content/classical-mechanics/kinematics/phase-6.md
  - .planning/phases/10-manual-pilot-node/SPEC-GAPS.md
autonomous: true
requirements:
  - PILOT-01

must_haves:
  truths:
    - "All 7 phase files contain textbook-correct physics with no placeholder text"
    - "Phase 1 struggle problem is genuinely solvable with pre-calculus but not optimally — Part C does NOT telegraph the integration answer"
    - "Phase 2 derivation explicitly states constant acceleration assumption and derives kinematic equations from definitions via integration"
    - "Phase 5 has 4+ quiz items mixing multiple_choice and fill_in_formula types spanning remember/understand/apply"
    - "Phase 5 contains a Transfer Problem section applying kinematics in a novel context"
    - "node.yaml phase 5 requires includes transfer_problem"
    - "Spec gaps discovered during authoring are collected in SPEC-GAPS.md (not applied to spec mid-authoring)"
  artifacts:
    - path: "content/classical-mechanics/kinematics/node.yaml"
      provides: "Updated phase manifest with transfer_problem in phase 5 requires"
      contains: "transfer_problem"
    - path: "content/classical-mechanics/kinematics/phase-1.md"
      provides: "Rewritten productive failure problem"
      min_lines: 40
    - path: "content/classical-mechanics/kinematics/phase-5.md"
      provides: "Quiz items + transfer problem"
      contains: "## Transfer Problem"
    - path: ".planning/phases/10-manual-pilot-node/SPEC-GAPS.md"
      provides: "Collected spec gaps and ambiguities"
      min_lines: 5
  key_links:
    - from: "content/classical-mechanics/kinematics/node.yaml"
      to: "content/classical-mechanics/kinematics/phase-5.md"
      via: "requires list declares transfer_problem; phase-5.md has ## Transfer Problem H2"
      pattern: "transfer_problem"
    - from: "docs/content-spec.md"
      to: "content/classical-mechanics/kinematics/"
      via: "All phase files conform to spec heading conventions and quiz block format"
      pattern: "phase_type"
---

<objective>
Rewrite all 7 phases of the kinematics pilot node to textbook-correct, pedagogically rigorous standards, and collect any content spec gaps discovered during authoring.

Purpose: Validate the 7-phase content template end-to-end by hand-authoring one complete node before building AI tooling (PILOT-01). The Phase 9 fixture was authored to test ingest mechanics — this rewrite raises it to genuine educational quality.

Output: 7 rewritten phase Markdown files, updated node.yaml, and a SPEC-GAPS.md findings file for the batch spec update in Plan 02.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-manual-pilot-node/10-CONTEXT.md
@.planning/phases/10-manual-pilot-node/10-RESEARCH.md
@docs/content-spec.md

<interfaces>
<!-- Key types from crates/domain/src/content_spec.rs -->

From node.yaml schema (serde target):
- concept_id: String (must match directory name "kinematics")
- eqf_level: u8 (value: 4 — triggers derivation_required: true)
- bloom_minimum: BloomLevel enum (value: apply)
- misconceptions: Vec<String> (must have 2-3 items)
- phases: Vec<PhaseEntry> — exactly 7 entries, numbers 0-6
- Each PhaseEntry: { number: u8, phase_type: String, requires: Vec<String> }

Phase Markdown frontmatter:
- phase: integer (0-6, must match filename)
- type: string (must match phase_type in node.yaml)
- estimated_minutes: integer

H2 heading convention:
- snake_case in requires -> Title Case H2 heading
- recall_prompt -> ## Recall Prompt
- transfer_problem -> ## Transfer Problem

Quiz block format (fenced ```quiz):
- type: multiple_choice | fill_in_formula | matching
- prompt: string (single-quoted if contains LaTeX backslashes)
- options: list[string] (for multiple_choice)
- answer: 0-based index (multiple_choice) or expression string (fill_in_formula)
- difficulty: remember | understand | apply | analyze | evaluate | create
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite phases 0-4 and update node.yaml</name>
  <files>
    content/classical-mechanics/kinematics/node.yaml,
    content/classical-mechanics/kinematics/phase-0.md,
    content/classical-mechanics/kinematics/phase-1.md,
    content/classical-mechanics/kinematics/phase-2.md,
    content/classical-mechanics/kinematics/phase-3.md,
    content/classical-mechanics/kinematics/phase-4.md
  </files>
  <read_first>
    docs/content-spec.md,
    content/classical-mechanics/kinematics/node.yaml,
    content/classical-mechanics/kinematics/phase-0.md,
    content/classical-mechanics/kinematics/phase-1.md,
    content/classical-mechanics/kinematics/phase-2.md,
    content/classical-mechanics/kinematics/phase-3.md,
    content/classical-mechanics/kinematics/phase-4.md,
    .planning/phases/10-manual-pilot-node/10-CONTEXT.md,
    .planning/phases/10-manual-pilot-node/10-RESEARCH.md
  </read_first>
  <action>
    Update node.yaml: add `transfer_problem` to phase 5 requires list (per D-09 known gap from research). The phase 5 requires should become:
    ```yaml
    - number: 5
      phase_type: retrieval_check
      requires:
        - quiz
        - transfer_problem
    ```
    All other node.yaml fields remain unchanged. Do NOT modify eqf_level, misconceptions, prerequisites, etc.

    Rewrite phase-0.md (Schema Activation) — per research, existing content is close to pilot quality. Polish:
    - Recall Prompt: keep the open question style; ensure it asks the learner to recall specific prerequisite concepts (vectors, calculus) and how they relate to describing motion
    - Linkage Map: explicit forward/backward links with LaTeX notation. Backward links to `vectors` and `calculus` prerequisites. Forward links to `projectile-motion`, `circular-motion`, `newtons-second-law`
    - Wonder Hook: an intriguing real-world scenario that kinematics explains (e.g., "How does a GPS satellite predict where it will be in 12 hours using only its current position, velocity, and the knowledge that acceleration is constant?")
    - Frontmatter: `phase: 0`, `type: schema_activation`, `estimated_minutes: 5`

    Rewrite phase-1.md (Productive Struggle) — THIS IS THE HIGHEST-RISK PIECE (per D-05, Pitfall 1):
    - Struggle Problem: Design a problem that is genuinely solvable with arithmetic/algebra + physics intuition (no calculus), but where the naive approach gives an imprecise answer. The learner should compute an estimate, NOT be guided toward integration.
      - CONCRETE DESIGN: A rocket accelerates from rest. Velocity readings are given at 1-second intervals for 5 seconds (non-constant acceleration — values like 0, 4, 10, 18, 28 m/s). Part A: "Estimate total distance traveled using only arithmetic." Part B: "Your friend says the average velocity times total time gives the exact distance. Is your friend right? Why or why not?" Part C: "Commit to your best estimate and explain your reasoning. What are you most uncertain about?"
      - CRITICAL: Part C must NOT say "describe the mathematical process" or hint at integration/calculus. It asks the learner to commit to their estimate and articulate uncertainty. The gap between their estimate and the exact answer is what motivates learning kinematic equations.
    - Solution Capture: prompt to record their numerical estimate, the method used (e.g., "I multiplied each velocity by 1 second and added"), and what assumptions they made about velocity between measurement points
    - Gap Reveal: explain that their rectangular approximation (whether left-sum, right-sum, or midpoint) gives different answers depending on the choice, and that kinematics provides exact equations for the special case of constant acceleration. For non-constant acceleration, the general tool is integration — but that is beyond this node's scope.
    - Frontmatter: `phase: 1`, `type: productive_struggle`, `estimated_minutes: 10`

    Rewrite phase-2.md (Concreteness Fading) — per research, existing derivation is close to pilot quality:
    - Concrete Stage: a specific real-world example with numbers (e.g., a car accelerating from 5 m/s at 2 m/s^2 for 3 seconds — compute final velocity and displacement using arithmetic and a velocity-time graph area)
    - Bridging Stage: replace specific numbers with named physical quantities ($v_0$, $a$, $t$) but keep the velocity-time graph area interpretation. Show the trapezoid area formula becoming $\Delta x = v_0 t + \frac{1}{2}at^2$
    - Abstract Stage: full symbolic kinematic equations in standard notation:
      $v = v_0 + at$
      $x = x_0 + v_0 t + \frac{1}{2}at^2$
      $v^2 = v_0^2 + 2a\Delta x$
    - Derivation (required at EQF 4, per D-04): Rigorous calculus-based derivation from definitions.
      - MUST explicitly state: "We assume acceleration $a$ is constant over the entire time interval" (per Pitfall 5)
      - Start from definition: $a = \frac{dv}{dt} = \text{const}$
      - Integrate: $\int_{v_0}^{v} dv' = \int_0^t a\,dt' \Rightarrow v = v_0 + at$
      - Then: $v = \frac{dx}{dt}$, integrate: $\int_{x_0}^{x} dx' = \int_0^t (v_0 + at')\,dt' \Rightarrow x = x_0 + v_0 t + \frac{1}{2}at^2$
      - Derive the third equation by eliminating $t$: from $t = (v - v_0)/a$, substitute into position equation to get $v^2 = v_0^2 + 2a(x - x_0)$
    - Use `$$...$$` for display math, `$...$` for inline. No double-quoted YAML strings with backslashes (per Pitfall 2).
    - Frontmatter: `phase: 2`, `type: concreteness_fading`, `estimated_minutes: 12`

    Rewrite phase-3.md (Worked Examples) — per research, needs richer context:
    - Full Example: A complete worked problem with physical context (e.g., "An airplane must reach 80 m/s to take off. The runway is 1600 m long. What minimum constant acceleration is needed?"). Show every step: identify knowns/unknowns, choose equation, solve algebraically, substitute numbers, check units and reasonableness.
    - Partially Faded Example: Same problem structure but different context (e.g., "A cyclist accelerates from rest..."). Show the setup and equation selection, but leave algebraic manipulation and numerical substitution as blanks marked with $\boxed{?}$ for the learner to fill in.
    - Mostly Faded Example (required at EQF 3+): Only give the problem statement and final numerical answer. The learner must produce the entire solution independently. (e.g., "A stone is dropped from a 45 m cliff. How long until it hits the ground? Answer: 3.03 s")
    - Frontmatter: `phase: 3`, `type: worked_examples`, `estimated_minutes: 10`

    Rewrite phase-4.md (Self-Explanation):
    - Self Explanation Prompt: ask the learner to explain IN THEIR OWN WORDS why constant acceleration is the key assumption that makes all three kinematic equations work, and what would change if acceleration varied with time
    - Reflection Questions (2-3 targeted questions):
      1. "Why does the third kinematic equation ($v^2 = v_0^2 + 2a\Delta x$) not contain time? In what physical situations is this particularly useful?"
      2. "Sign conventions: if you define upward as positive and drop a ball, what signs do $v_0$, $a$, and $\Delta x$ have? What happens if you accidentally use $a = +9.8$ instead of $a = -9.8$?"
      3. "The derivation assumed constant $a$. Name one real-world situation where this assumption clearly fails and explain why the kinematic equations would give wrong answers."
    - Frontmatter: `phase: 4`, `type: self_explanation`, `estimated_minutes: 6`

    While authoring, start a mental list of spec gaps or ambiguities encountered. Do NOT modify docs/content-spec.md or crates/domain/src/content_spec.rs in this task.
  </action>
  <verify>
    <automated>cd /home/jasper/Repositories/physics-tree && cargo run --bin validate --features ssr -- content/classical-mechanics/kinematics 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - node.yaml phase 5 requires contains `transfer_problem` (grep: `- transfer_problem`)
    - phase-0.md contains `## Recall Prompt`, `## Linkage Map`, `## Wonder Hook` H2 headings
    - phase-1.md contains `## Struggle Problem`, `## Solution Capture`, `## Gap Reveal` H2 headings
    - phase-1.md does NOT contain the phrase "describe the mathematical process" or "what would you need to compute"
    - phase-1.md Part C asks learner to "commit" to their estimate (grep: `commit` or `Commit`)
    - phase-2.md contains `## Derivation` H2 heading
    - phase-2.md contains explicit constant acceleration assumption (grep: `assume` or `constant` near `acceleration`)
    - phase-2.md contains integral notation (grep: `\\int` or `\int`)
    - phase-3.md contains `## Full Example`, `## Partially Faded Example`, `## Mostly Faded Example`
    - phase-3.md contains `\boxed{?}` in the partially faded example
    - phase-4.md contains `## Self Explanation Prompt` and `## Reflection Questions`
    - All frontmatter uses single-quoted or literal block scalar for any LaTeX-containing strings
    - validate CLI exits 0 (node is structurally valid)
  </acceptance_criteria>
  <done>Phases 0-4 rewritten to textbook-correct pilot quality. node.yaml updated with transfer_problem. Validator passes.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite phases 5-6 and create SPEC-GAPS.md</name>
  <files>
    content/classical-mechanics/kinematics/phase-5.md,
    content/classical-mechanics/kinematics/phase-6.md,
    .planning/phases/10-manual-pilot-node/SPEC-GAPS.md
  </files>
  <read_first>
    docs/content-spec.md,
    content/classical-mechanics/kinematics/node.yaml,
    content/classical-mechanics/kinematics/phase-5.md,
    content/classical-mechanics/kinematics/phase-6.md,
    .planning/phases/10-manual-pilot-node/10-CONTEXT.md,
    .planning/phases/10-manual-pilot-node/10-RESEARCH.md
  </read_first>
  <action>
    Rewrite phase-5.md (Retrieval Check) — per D-06, must have 4+ quiz items mixing types and difficulty:
    - Quiz section with at least 5 quiz blocks:
      1. `multiple_choice`, difficulty `remember`: "Which of the following is the correct kinematic equation relating velocity, initial velocity, and acceleration?" (options: the 3 kinematic equations + 1 distractor)
      2. `fill_in_formula`, difficulty `remember`: "Write the kinematic equation for displacement as a function of time." Answer: `x = x_0 + v_0*t + (1/2)*a*t^2`
      3. `multiple_choice`, difficulty `understand`: "A ball is thrown straight up. At the very top of its path, which statement is true?" (options about velocity=0 but acceleration != 0, common misconception traps)
      4. `multiple_choice`, difficulty `apply`: "A car brakes uniformly from 25 m/s to rest over 50 m. What is the magnitude of its deceleration?" (requires using $v^2 = v_0^2 + 2a\Delta x$)
      5. `fill_in_formula`, difficulty `apply`: "A ball is dropped from rest and falls for 3 s ($g = 9.8\,\text{m/s}^2$). Write the expression for how far it falls." Answer: `(1/2)*9.8*3^2` or equivalent (44.1 m)
    - CRITICAL for quiz YAML: use single-quoted strings for ANY prompt or option containing backslashes (per Pitfall 2). Use `\\,` for thin spaces in double-quoted strings, or preferably switch to single quotes.
    - Transfer Problem section (## Transfer Problem): A novel context NOT used in worked examples.
      CONCRETE DESIGN: "A diver jumps upward from a 10 m platform with an initial speed of 2 m/s. (a) How high above the water does the diver reach? (b) How long until the diver hits the water? (c) At what speed does the diver enter the water?" This combines vertical free-fall with a non-zero initial condition and a displacement that is negative relative to the starting point — a transfer requiring careful sign convention work.
    - Frontmatter: `phase: 5`, `type: retrieval_check`, `estimated_minutes: 12`

    Rewrite phase-6.md (Spaced Return):
    - Spaced Prompt: "Without looking at notes, derive the three kinematic equations starting from the definition $a = dv/dt = \text{const}$. Then state the one key assumption and name a situation where it fails."
    - Interleaving Problem: combine kinematics with vectors prerequisite — e.g., "A ball is launched at 20 m/s at 30 degrees above horizontal. Find the time of flight, maximum height, and range. You will need vector decomposition from the vectors node." Include enough setup that the learner must decompose $v_0$ into $v_{0x}$ and $v_{0y}$ components and apply kinematics independently to each axis.
    - Frontmatter: `phase: 6`, `type: spaced_return`, `estimated_minutes: 8`

    Create SPEC-GAPS.md in the phase planning directory. This is the findings list per D-09. Include AT MINIMUM these known gaps from research:
    ```
    # Spec Gaps Found During Pilot Authoring

    ## Gap 1: transfer_problem not enforced by validator
    - **Where:** node.yaml phase 5 requires
    - **Issue:** The spec (docs/content-spec.md Section 4, Phase 5) lists `transfer_problem` as a standard required block, but the validator only checks that declared requires have matching H2 headings. It does NOT check that standard blocks are declared.
    - **Recommendation:** Add a validation rule that checks phase 5 requires includes `transfer_problem` for all nodes. Similar checks could enforce other standard blocks per phase.
    - **Severity:** HIGH — a node could pass validation while missing a spec-mandated section

    ## Gap 2: \boxed{?} placeholder convention undocumented
    - **Where:** Phase 3 partially faded examples
    - **Issue:** The spec does not define how learner-fill blanks should be marked in worked examples. The pilot uses `\boxed{?}` but this is not in the spec.
    - **Recommendation:** Add a "Conventions" subsection to the Phase 3 spec documenting `\boxed{?}` as the standard blank marker.
    - **Severity:** LOW — cosmetic, but AI pipeline needs a defined convention

    ## Gap 3: esco_tags can be empty without validator complaint
    - **Where:** node.yaml
    - **Issue:** esco_tags: [] passes validation. The spec says the field is required but does not specify minimum count.
    - **Recommendation:** Leave as-is for now — ESCO tags are a Phase 14 concern. Document that empty is acceptable during pilot phase.
    - **Severity:** LOW — not blocking for PILOT-01
    ```
    Also add any additional gaps discovered during the authoring of Tasks 1 and 2 (there may be more). The SPEC-GAPS.md file will be consumed by Plan 02.
  </action>
  <verify>
    <automated>cd /home/jasper/Repositories/physics-tree && cargo run --bin validate --features ssr -- content/classical-mechanics/kinematics 2>&1 && echo "---" && grep -c "^## Gap" .planning/phases/10-manual-pilot-node/SPEC-GAPS.md</automated>
  </verify>
  <acceptance_criteria>
    - phase-5.md contains `## Quiz` and `## Transfer Problem` H2 headings
    - phase-5.md contains at least 5 ` ```quiz ` fenced blocks (grep count of "```quiz" >= 5)
    - phase-5.md quiz blocks include at least 1 `type: fill_in_formula` and at least 2 `type: multiple_choice`
    - phase-5.md quiz blocks span at least 2 distinct difficulty levels (e.g., `remember` and `apply`)
    - phase-5.md Transfer Problem involves a physical scenario not used in phase-3 worked examples
    - phase-6.md contains `## Spaced Prompt` and `## Interleaving Problem`
    - phase-6.md interleaving problem references vector decomposition (grep: `component` or `decompos`)
    - SPEC-GAPS.md exists and contains at least 3 `## Gap` headings
    - SPEC-GAPS.md Gap 1 mentions `transfer_problem` validator enforcement
    - validate CLI exits 0 after all changes
  </acceptance_criteria>
  <done>Phases 5-6 rewritten with 5+ mixed quiz items and transfer problem. SPEC-GAPS.md documents all gaps found during authoring. Full node passes validation.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `cargo run --bin validate --features ssr -- content/classical-mechanics/kinematics` exits 0
2. All 7 phase files have no placeholder text (no "TODO", "TBD", "placeholder", "lorem")
3. node.yaml phase 5 requires includes both `quiz` and `transfer_problem`
4. SPEC-GAPS.md exists with documented findings
</verification>

<success_criteria>
- All 7 phases rewritten to textbook-correct kinematics content with proper LaTeX notation
- Phase 1 productive failure problem does not telegraph integration as the solution method
- Phase 2 derivation uses calculus from first principles with explicit constant-acceleration assumption
- Phase 5 has 5+ quiz items spanning remember/understand/apply difficulties with multiple_choice and fill_in_formula types
- Phase 5 includes a transfer problem in a novel physical context
- Kinematics node passes the validate CLI
- SPEC-GAPS.md catalogs all spec ambiguities discovered during authoring
</success_criteria>

<output>
After completion, create `.planning/phases/10-manual-pilot-node/10-01-SUMMARY.md`
</output>
