---
phase: 10-manual-pilot-node
plan: 02
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - docs/content-spec.md
  - crates/domain/src/content_spec.rs
autonomous: false
requirements:
  - PILOT-01

must_haves:
  truths:
    - "Spec gaps found during pilot authoring are resolved in both the spec document and validator code"
    - "The kinematics node passes the UPDATED validator (not just the old one)"
    - "The kinematics node passes ingest --dry-run confirming database schema compatibility"
    - "Human has reviewed the pilot node content for physics accuracy and pedagogical quality"
  artifacts:
    - path: "docs/content-spec.md"
      provides: "Updated content spec with gap resolutions"
    - path: "crates/domain/src/content_spec.rs"
      provides: "Updated validation code with new rules"
  key_links:
    - from: "crates/domain/src/content_spec.rs"
      to: "content/classical-mechanics/kinematics/"
      via: "validate_node() enforces new rules; kinematics node passes"
      pattern: "validate_node"
    - from: ".planning/phases/10-manual-pilot-node/SPEC-GAPS.md"
      to: "docs/content-spec.md"
      via: "Each gap in SPEC-GAPS.md has a resolution in the spec"
      pattern: "transfer_problem"
---

<objective>
Batch-apply spec and validator updates for gaps discovered during pilot authoring, then verify the kinematics node through the complete pipeline (validate + ingest --dry-run + human review).

Purpose: Close the template feedback loop (D-10, D-11) — the whole point of a manual pilot is to surface spec ambiguities, resolve them, and confirm the updated spec is mechanically enforceable before AI tooling is built.

Output: Updated content-spec.md, updated content_spec.rs validator, passing pipeline verification, and human sign-off on content quality.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-manual-pilot-node/10-CONTEXT.md
@.planning/phases/10-manual-pilot-node/10-RESEARCH.md
@.planning/phases/10-manual-pilot-node/SPEC-GAPS.md
@.planning/phases/10-manual-pilot-node/10-01-SUMMARY.md

<interfaces>
<!-- From crates/domain/src/content_spec.rs — validation architecture -->

Validation error pattern (collect-all, no early return):
```rust
pub enum ValidationError {
    YamlParse { file: String, detail: String },
    EqfRange { value: u8 },
    MisconceptionCount { count: usize },
    // ... existing variants
}

pub fn validate_node(node_dir: &Path) -> Vec<ValidationError> {
    let mut errors = Vec::new();
    // ... checks push to errors ...
    errors
}
```

To add a new validation rule:
1. Add a new variant to `ValidationError` enum
2. Add a corresponding arm in the `Display` impl
3. Add the check logic in `validate_node()` using `errors.push(ValidationError::NewVariant { ... })`
4. Add unit test: `assert!(errors.is_empty())` for valid case, `assert_eq!(errors.len(), N)` for invalid case

Existing test pattern in same file:
```rust
#[cfg(test)]
mod tests {
    use super::*;
    // Tests use tempdir with fixture files
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Batch-apply spec gap resolutions to spec document and validator</name>
  <files>
    docs/content-spec.md,
    crates/domain/src/content_spec.rs
  </files>
  <read_first>
    .planning/phases/10-manual-pilot-node/SPEC-GAPS.md,
    docs/content-spec.md,
    crates/domain/src/content_spec.rs,
    .planning/phases/10-manual-pilot-node/10-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: A node with phase 5 requires missing `transfer_problem` produces a ValidationError (RED: new variant `MissingStandardRequires` or similar)
    - Test 2: A node with phase 5 requires including `transfer_problem` passes (existing valid-node test still passes)
    - Test 3: The kinematics fixture node (with transfer_problem added in Plan 01) passes all validation including the new rule
  </behavior>
  <action>
    Read SPEC-GAPS.md produced by Plan 01 to get the full list of gaps. Apply resolutions per D-10:

    FOR GAP 1 (transfer_problem not enforced):
    1. In `docs/content-spec.md` Section 8 (Validation Rules), add check 13:
       "13. **Standard requires enforcement: `transfer_problem`** — Phase 5 `requires` must include `transfer_problem`"
    2. In `crates/domain/src/content_spec.rs`:
       - Add `ValidationError` variant: `MissingStandardRequires { phase: u8, block: String }` with Display message: `"node.yaml:phases[{phase}]  Missing standard required block '{block}' for phase type retrieval_check"`
       - In `validate_node()`, after existing requires checks, add: if phase.number == 5 && phase.phase_type == "retrieval_check" && !phase.requires.contains(&"transfer_problem".to_string()) { errors.push(ValidationError::MissingStandardRequires { phase: 5, block: "transfer_problem".into() }) }
       - Add unit test: create a fixture node with phase 5 requires = ["quiz"] only (no transfer_problem) and assert the new error is produced
       - Add unit test: create a fixture node with phase 5 requires = ["quiz", "transfer_problem"] and assert no error from this check

    FOR GAP 2 (boxed placeholder convention):
    1. In `docs/content-spec.md` Section 4, Phase 3 (Worked Examples), add a note under Partially Faded Example:
       "> **Blank marker convention:** Use `\boxed{?}` (KaTeX) to mark steps the learner should fill in. Each `\boxed{?}` replaces exactly one algebraic step or numerical substitution."
    2. No validator change needed — this is a documentation-only gap.

    FOR GAP 3 (esco_tags empty):
    1. In `docs/content-spec.md` Section 3, add a note under esco_tags:
       "> **Note:** Empty list `[]` is valid during pilot authoring. ESCO tag population is deferred to Phase 14."
    2. No validator change needed.

    FOR ANY ADDITIONAL GAPS in SPEC-GAPS.md: apply similar pattern — update spec doc, add validator rule if mechanically enforceable, add unit test for new rules.

    After all changes, run existing tests to ensure no regressions:
    ```bash
    cargo test -p domain --lib -- content_spec
    ```

    Then verify the pilot node still passes:
    ```bash
    cargo run --bin validate --features ssr -- content/classical-mechanics/kinematics
    ```
  </action>
  <verify>
    <automated>cd /home/jasper/Repositories/physics-tree && cargo test -p domain --lib -- content_spec 2>&1 && echo "---VALIDATE---" && cargo run --bin validate --features ssr -- content/classical-mechanics/kinematics 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - content_spec.rs contains `MissingStandardRequires` variant (or equivalent name for the transfer_problem enforcement error)
    - content_spec.rs validate_node() has a check for phase 5 transfer_problem (grep: `transfer_problem`)
    - content_spec.rs has at least 1 new unit test for the transfer_problem enforcement
    - docs/content-spec.md mentions `transfer_problem` enforcement in Validation Rules section
    - docs/content-spec.md contains `\boxed{?}` blank marker convention text
    - `cargo test -p domain --lib -- content_spec` exits 0 (all tests pass, including new ones)
    - `cargo run --bin validate --features ssr -- content/classical-mechanics/kinematics` exits 0 (pilot node passes updated validator)
  </acceptance_criteria>
  <done>Spec document and validator updated for all gaps. New validation rules have unit tests. Pilot node passes updated validator. No test regressions.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human review of pilot node content and pipeline verification</name>
  <files>
    content/classical-mechanics/kinematics/phase-0.md,
    content/classical-mechanics/kinematics/phase-1.md,
    content/classical-mechanics/kinematics/phase-2.md,
    content/classical-mechanics/kinematics/phase-3.md,
    content/classical-mechanics/kinematics/phase-4.md,
    content/classical-mechanics/kinematics/phase-5.md,
    content/classical-mechanics/kinematics/phase-6.md
  </files>
  <read_first>
    content/classical-mechanics/kinematics/phase-0.md,
    content/classical-mechanics/kinematics/phase-1.md,
    content/classical-mechanics/kinematics/phase-2.md,
    content/classical-mechanics/kinematics/phase-3.md,
    content/classical-mechanics/kinematics/phase-4.md,
    content/classical-mechanics/kinematics/phase-5.md,
    content/classical-mechanics/kinematics/phase-6.md,
    content/classical-mechanics/kinematics/node.yaml
  </read_first>
  <action>
    Before presenting this checkpoint to the human, run the full pipeline verification per D-08:
    1. `cargo run --bin validate --features ssr -- content/classical-mechanics/kinematics` — must exit 0
    2. `cargo run --bin ingest --features ssr -- content/classical-mechanics/kinematics --dry-run` — must exit 0
    3. If DATABASE_URL is set: `cargo run --bin ingest --features ssr -- content/classical-mechanics/kinematics` — actual ingest to database
    Present the pipeline output to the human alongside the content review request.
  </action>
  <what-built>
    Complete kinematics pilot node with all 7 phases rewritten to textbook-correct, pedagogically rigorous standards. The node passes structural validation and the updated validator. This checkpoint covers:
    1. Physics accuracy review (D-04): formulas, derivations, units
    2. Productive failure quality (D-05): Phase 1 struggle problem design
    3. Quiz quality (D-06): Phase 5 quiz items
    4. Pipeline verification (D-08): validate + ingest --dry-run
  </what-built>
  <how-to-verify>
    1. Review physics accuracy in all 7 phase files:
       - `content/classical-mechanics/kinematics/phase-0.md` through `phase-6.md`
       - Check all formulas, derivations, and units for correctness
       - Verify misconceptions listed are genuine student beliefs

    2. Review Phase 1 productive failure design:
       - Read phase-1.md Struggle Problem carefully
       - Confirm: Is the problem solvable with arithmetic/algebra only? YES required.
       - Confirm: Does Part C avoid telegraphing integration/calculus as the answer? YES required.
       - Confirm: Is there a genuine gap between the naive estimate and the exact answer? YES required.

    3. Review Phase 5 quiz items:
       - Verify 4+ items with mixed types (multiple_choice + fill_in_formula)
       - Verify difficulty span (remember, understand, apply)
       - Check that distractors are plausible but clearly wrong

    4. Verify pipeline output:
       - Executor will have already run validate + ingest --dry-run
       - Check that both commands produced clean output (no warnings)

    5. If database is available, confirm actual ingest:
       - `cargo run --bin ingest --features ssr -- content/classical-mechanics/kinematics`
       - Verify phases appear in the node_phases table
  </how-to-verify>
  <verify>
    <automated>cd /home/jasper/Repositories/physics-tree && cargo run --bin validate --features ssr -- content/classical-mechanics/kinematics 2>&1 && cargo run --bin ingest --features ssr -- content/classical-mechanics/kinematics --dry-run 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - validate CLI exits 0 for kinematics node
    - ingest --dry-run exits 0 for kinematics node
    - Human confirms physics accuracy of all 7 phases
    - Human confirms Phase 1 productive failure design meets D-05 criteria
    - Human confirms Phase 5 has 4+ mixed quiz items per D-06
  </acceptance_criteria>
  <done>Human approves pilot node content quality. Pipeline verification (validate + ingest --dry-run) passes. Phase 10 PILOT-01 requirement is satisfied.</done>
  <resume-signal>Type "approved" to complete Phase 10, or describe specific issues to fix</resume-signal>
</task>

</tasks>

<verification>
After both tasks complete:
1. `cargo test -p domain --lib -- content_spec` exits 0 (all validator tests pass)
2. `cargo run --bin validate --features ssr -- content/classical-mechanics/kinematics` exits 0 (pilot node valid)
3. `cargo run --bin ingest --features ssr -- content/classical-mechanics/kinematics --dry-run` exits 0 (ingest compatible)
4. Human has reviewed and approved content quality
5. SPEC-GAPS.md gaps are all addressed in spec + validator
</verification>

<success_criteria>
- Every gap in SPEC-GAPS.md is resolved in docs/content-spec.md and (where mechanically enforceable) in content_spec.rs
- New validator rules have unit tests
- Pilot node passes the updated validator AND ingest --dry-run
- Human approves physics accuracy, productive failure design, and quiz quality
- Phase 10 success criteria from ROADMAP are met: one node exists with all 7 phases, passes ingest, and spec ambiguities are resolved
</success_criteria>

<output>
After completion, create `.planning/phases/10-manual-pilot-node/10-02-SUMMARY.md`
</output>
