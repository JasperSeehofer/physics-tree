# M11d — Independent Adversarial Review (law D10), S0.5 nodes 4 and 5

**Sub-mission:** M11d, independent review of the M11b drafts of nodes 4 and 5, and staging on a clean result.
**Branch:** `mission/M11-s05-nodes-2-5`. **Worktree:** `~/Repositories/pt-M11`. Never pushed, never merged. Main checkout and `pt-M12` never touched.
**Method:** re-derive, don't read. Every equation and every number below was derived or recomputed independently from the branch's stated conventions (node 1's Phase-2 Conventions table, as staged) *before* being compared with the draft. Reviewer is neither the author (M11b) nor the sibling reviewer (M11c); M11c's report was read for its conventions findings and its Gate-8 items, not for its verdicts. Ingest not run (law 7).

## Verdict

| Node | Slug | Verdict | MAJOR | MINOR | NOTE | Staged |
|---|---|---|---|---|---|---|
| 4 | `hilbert-space-for-fields-and-continuum-normalization` | **PASS with 2 MINORs, both fixed in place** | 0 | 2 | 5 | ✅ `content/quantum-field-theory/` |
| 5 | `lorentz-invariant-measure-and-normalization-conventions` | **PASS with 2 MAJORs and 1 MINOR, all fixed in place** | 2 (0 unresolved) | 1 | 5 | ✅ `content/quantum-field-theory/` |

**Zero unresolved MAJORs.** Both nodes staged, in dependency order (4, then 5).

Node 5's two MAJORs are of the class this gate exists for: both are statements the node makes about *itself* that its own later sections refute, and both sit in the Phase-0 framing the learner reads first and carries forward. Neither was on the author's seven-item scrutiny list; both survived the author's own three trim passes. The author's scrutiny list was worked through in full and, apart from the items below, every item on it checked out.

---

## 1. Findings

### Node 4 — `hilbert-space-for-fields-and-continuum-normalization`

| # | Severity | Location | Finding | Fix applied |
|---|---|---|---|---|
| F-4.1 | **MINOR** (wrong object) | phase-1, Gap Reveal, Part B1–B2 | *"$\langle x\lvert x\rangle = \delta(0)$, not a number; $[\delta(x)]$ is an inverse length, and in three dimensions $\delta^{3}(0) = V/(2\pi)^{3}$, a **volume**."* The two clauses name **different deltas** and the sentence is self-contradicting: the object in $\langle x\lvert x\rangle$ is the *position*-space delta at coincidence, $\delta^{3}(\mathbf{x}=0)=\int\!d^{3}k/(2\pi)^{3}$ — an infinite **inverse** volume, dimension $+3$, divergent in the **ultraviolet**. $V/(2\pi)^{3}$ is the *momentum*-space $\delta^{3}(\mathbf{k}=0)$, dimension $-3$, divergent in the **infrared**. The node's own Concrete Stage table classifies exactly these two as the two independent divergences ("cured by a finite box" vs "cured by a momentum cutoff"), and Part C2 uses $V/(2\pi)^{3}$ correctly for the momentum delta. In a node whose subject is telling these objects apart, the compressed segue teaches the merge it exists to prevent. | Rewritten to keep the 1D dimension statement and to name Part C's momentum-space delta as the *other* object: "…so this is an infinite inverse length — and *not* Part C's momentum-space $\delta^{3}(0) = V/(2\pi)^{3}$, which is a volume." Paid for by trimming the following sentence in the same paragraph. |
| F-4.2 | **MINOR** (measured-fidelity drift, F-3.4 class) | phase-0, Wonder Hook | The `[MEASURED]` on-ramp called E2's completeness insertion *"one of exactly three in the whole **five-block assessment**, alongside the Levi-Civita formula and the geometric-trinity triangle."* The vault attests the "clean productions" list (`qg-knowledge-state.md`, Block E consequences) — but the **Levi-Civita formula is not from a block**: it is item 4 of the `lie-vs-covariant-derivative` **node-telemetry probe** of 2026-08-15, recorded there as "(third clean production)". The vault's own numbering also implies the list had members before it, so "exactly three" over-specifies a count the record does not fix. M10a §4 makes no such claim; the drift was introduced at authoring — the same map→author path as F-3.4. | Narrowed to "one of only three **across the whole assessment record**", which is what the vault supports. (Routing rule 3's "one of three clean productions in the whole assessment" is left: it makes no block claim.) |
| N-4.1 | NOTE → fixed | phase-3, Full Example Step 6 | *"the fourth and second columns of the table are the same number"* is ambiguous — $N$ and $\lvert\langle\mathbf{k}_{0}\lvert f_{\sigma}\rangle\rvert$ are the table's **third and fifth** columns counting the $\sigma$ label, second and fourth counting only data columns. The identity itself is correct and verified. | Replaced with "the $N$ and overlap columns", which is unambiguous and three words shorter — it is what paid for F-4.1. |
| N-4.2 | NOTE | phase-2, Abstract Stage reading 4 | *"$\mathcal{H}_{1}$ is separable — it has a countable orthonormal basis (**the box modes**)."* Separability is right and the point stands; the parenthetical is loose, since the box modes are an orthonormal basis of $L^{2}$ **of the box**, not of $L^{2}(\mathbb{R}^{3})$. A correct exemplar would be the Hermite functions. Not fixed: node 4 had two words of headroom after F-4.1, the claim it supports is true, and the box is the node's own running device. | No change |
| N-4.3 | NOTE | phase-5, Transfer Problem (c) | The setup is a **massless scalar** on Schwarzschild (the $s=0$ Regge–Wheeler potential is written correctly), and part (c) then quotes the fundamental $\ell = 2$ **gravitational** QNM, $M\omega \approx 0.3737 - 0.0890\,i$. The switch is explicitly labelled "gravitational" and the value is the correct tabulated one; the scalar $\ell=2$ value would be $\approx 0.4836 - 0.0968\,i$. Recomputed part (f) from the quoted value: $60\,M_{\odot}$ gives $f = 201$ Hz and $\tau = 3.3$ ms ✅, and the ringdown framing needs the gravitational number to be the LIGO-band statement it claims. Deliberate and defensible; recorded so a later reviewer does not read it as a slip. | No change |
| N-4.4 | NOTE | phase-0 routing rule 2, Wonder Hook | The Block E sheet's answer is recorded verbatim in the vault as *"because it is 4D"*; the node quotes it, inside quotation marks and as a blockquote, expanded to *"because it is four-dimensional"*. `node.yaml` carries the verbatim form. A faithful expansion rather than a content change — recorded only because F-4.2 shows this layer is where drift enters. | No change |
| N-4.5 | NOTE | phase-0, Calibration Probe | Three probe items against the spec's 4–6 reference range. See the **probe-range ruling** in §3; the same ruling covers node 5. M10a §4's probe sketch for node 4 specifies exactly these three. | No change |

### Node 5 — `lorentz-invariant-measure-and-normalization-conventions`

| # | Severity | Location | Finding | Fix applied |
|---|---|---|---|---|
| F-5.1 | **MAJOR** (wrong source attribution + contradicts the node's own conclusion) | phase-0, Wonder Hook | The three-way "where the $\sqrt{2E}$ can live" list read: *"**in the measure**, leaving $\lvert\mathbf{k}\rangle = a^{\dagger}_{\mathbf{k}}\lvert0\rangle$ — **this branch, following Peskin**"*, then "in the state — the relativistically normalized convention", then "in the operator — Srednicki", with *"All three are used in print, all three are correct."* Three things are wrong at once. **(i) Peskin does not do this**: P&S 2.35 defines $\lvert p\rangle = \sqrt{2E_{\mathbf{p}}}\,a^{\dagger}_{\mathbf{p}}\lvert0\rangle$ — the $\sqrt{2E}$ is in the state, which is exactly what the node's own Bridging Stage row 1 and Full Example row 1 (both labelled "This branch (Peskin)", both with $S=\sqrt{2E_{\mathbf{k}}}$) say. **(ii) It contradicts the branch's own decision**: D3 proves and the Derivation's Conventions table declares $\lvert\mathbf{k}\rangle_{R} = \sqrt{2E_{\mathbf{k}}}a^{\dagger}_{\mathbf{k}}\lvert0\rangle$ as the row this node fills, and lists $a^{\dagger}_{\mathbf{k}}\lvert0\rangle$ under "Also common, and **incompatible** … **not** covariant". **(iii) The bullet is not a convention at all**: $S=1$ with $C=(2\pi)^{3}$ gives $\lvert S\rvert^{2}C=(2\pi)^{3}$, violating the node's own identity — and it is verbatim **Source C** of the Mostly Faded forensics, whose expected answer is "**Source C is inconsistent** … do not import from C". A learner who read the Wonder Hook would classify the branch itself as the source the node forbids importing from, and would carry away the misattribution of Peskin in the node whose declared `convention_trap` is source interference. | Replaced with the $(S,C)$ trichotomy that matches the Bridging Stage and Full Example tables exactly — $(\sqrt{2E},(2\pi)^{3})$ Peskin = this branch; $(\sqrt{(2\pi)^{3}2E},1)$ symmetric transform; $(1,(2\pi)^{3}2E)$ Srednicki — followed by an explicit paragraph saying that $\lvert\mathbf{k}\rangle=a^{\dagger}_{\mathbf{k}}\lvert0\rangle$ with node 2's commutator is the **placeholder nodes 1–4 have been writing while the row stood open**, not a fourth convention, and that Phase 3 meets it again as a source not to import from. |
| F-5.2 | **MAJOR** (false claim, repeated in five places, contradicts staged node 1) | phase-0 Wonder Hook · phase-1 Gap Reveal C5 · phase-2 Concrete Number 3 · phase-3 Full Example Step 4 · phase-5 quiz item 6 option 0 | The node asserted, as "the sharp point", that a mixed state normalization **"does not fail a dimensional check cleanly"**, reasoning that "the same mismatch shifts the mass dimension of $\mathcal{M}$ by exactly as much as it shifts the cross section". That reasoning refutes its own conclusion: if the cross section's mass dimension shifts, a dimensional check catches it *cleanly*. And it does shift — the node itself prices the error as $\prod_{i}2E_{i} = (90\ \mathrm{GeV})^{4} = 6.6\times10^{7}\ \mathrm{GeV}^{4}$, i.e. an error carrying **mass dimension four**, so the output has dimension $+2$ where a cross section has $-2$ and is not an area at all. It also contradicts **staged node 1**, whose Conventions block states in as many words that "the cheapest available defence is the dimensional check … within either convention the mass dimension … comes out the same by two independent routes, and across a mixed pair it does not". Two consequential downstream errors followed: phase-2 Number 3 said the wrong answer "disagrees with the measured cross section by **seven orders of magnitude**" — a dimensionful factor is not an order of magnitude — and phase-1's struggle item 5 asks the learner "whether a dimensional check would catch it" with a wrong answer key, teaching distrust of the one cheap check that works. | Corrected in all five places to the statement that is true and that keeps the author's real point intact: the factor is large, energy-dependent **and dimensionful**, so what comes out is not a cross section and a dimensional check **on the final answer does catch it** (node 1's defence, cited); what nothing on the page reveals is *where* it entered, because every symbol is the symbol its source wrote. Framed as **detection is cheap, localization is what $\lvert S\rvert^{2}C$ is for** — which strengthens the node's Step 5 rather than weakening it, and leaves the "no correctness gate" rationale untouched (that rests on there being no single wrong probe answer, not on invisibility to dimensions). |
| F-5.3 | **MINOR** (wrong count about a staged node) | phase-4, Reflection Question 1(iii) | *"Node 1's convention table had **nine** rows and this node **added one**. Which of the ten…"* Node 1's staged table has **ten** rows (Units · Metric signature · Four-vectors · On-shell energy · Positive frequency · Fourier convention · Mode normalization · Ladder commutator · **State normalization** · Sign of $i$ in the CCR), the ninth being the one deliberately left blank with a note naming node 5. Node 5 *fills* it; it does not add an eleventh. The total of ten happens to be right, so the exercise still works — but a learner told to "sort the whole table" will count ten in node 1 and stop. | "…had ten rows, one of them — the state normalization — deliberately left blank, and this node is what fills it." |
| N-5.1 | NOTE → fixed | phase-5 banner | The banner says the map-mandated $\sqrt{-g}$ geometry-basin distractor "appears in items 1, 3 and 5"; audited item by item, it appears in **all six**. (M11b's notes say five of six.) Understatement only; the rule is satisfied either way. | Comment corrected to "all six items". |
| N-5.2 | NOTE | phase-0, Calibration Probe | Two probe items against the spec's 4–6 reference range, as M10a §4 specifies. **Ruling in §3: the range is reference-only.** | No change |
| N-5.3 | NOTE | phase-3, Partially Faded Step 6 | $\Gamma = \lvert\mathcal{M}\rvert^{2}\sqrt{1-4\mu^{2}/m^{2}}/(16\pi m)$ carries **no $\tfrac12$ identical-particle symmetry factor**. Correct as posed — the step says "equal daughter masses $\mu$", not identical particles — and re-derived to that form exactly. Recorded because the omission looks like one at a glance and node 6 will raise the symmetry factor. | No change |
| N-5.4 | NOTE | phase-2, Derivation, "A note on the retrofit" | M11b asked (scrutiny item 8a) whether $\langle\mathbf{k}\lvert\mathbf{k}\rangle = V \to 2E_{\mathbf{k}}V$ is the *only* earlier number that changes. It is not the only one — node 4's packet quantities ($N$, $\lvert\psi_{\sigma}(0)\rvert$, $\langle\mathbf{k}_{0}\lvert f_{\sigma}\rangle$) are all stated against node 4's measure and would be re-parametrised too. The node does **not** claim exclusivity: it states the general rule ("the covariant version carries an extra $2E_{\mathbf{k}}$ per state") and then gives one instance, and node 4's Conventions block independently promises that "every inner product below carries its convention explicitly". Verified: node 4's physics results are statements about $a_{\mathbf{k}}$, $\varphi$ and $\lvert0\rangle$ and are untouched. Claim stands as written. | No change |
| N-5.5 | NOTE | phase-2 Bridging, phase-3 Step 2 | The "Symmetric Fourier" row is a **constructed** convention, not a named source: it is normalized to the branch's $\langle\mathbf{k}\lvert\mathbf{k}'\rangle = (2\pi)^{3}2E\delta^{3}$, which forces $S = \sqrt{(2\pi)^{3}2E}$ — a real symmetric-transform source would more likely write $\langle\mathbf{k}\lvert\mathbf{k}'\rangle = 2E\delta^{3}$ and put no $(2\pi)^{3}$ anywhere. Internally consistent, labelled as a convention rather than a book, and it satisfies both branch identities. Recorded so the row is not read as an attribution. | No change |

---

## 2. Re-derivations performed

Everything below was derived or recomputed from scratch and only then compared. ✅ = draft agrees.

### Convention anchor (staged node 1 + node 2, re-verified before anything else)
- $\varphi=\int\frac{d^3k}{(2\pi)^3}\frac{1}{\sqrt{2\omega_k}}(a_ke^{-ikx}+a^\dagger_ke^{+ikx})$, $[a_k,a^\dagger_{k'}]=(2\pi)^3\delta^3$, $\pi=\dot\varphi$, $(+,-,-,-)$, $kx=k^0t-\mathbf k\cdot\mathbf x$. ✅ Node 1's table has **ten** rows, the state-normalization row blank and naming node 5 (→ F-5.3).
- Node 2's staged $P^{2}C\omega_{\mathbf k}=\tfrac12(2\pi)^{-3}$ and $[\varphi(x),\varphi(y)]=i\Delta$, $\Delta(z)=-\int\frac{d^3k}{(2\pi)^3\omega_k}\sin(kz)$ — both re-read from `content/` rather than from M11c's report. ✅

### Node 4
| Object | Independent result | |
|---|---|---|
| $\langle\mathbf k\lvert\mathbf k'\rangle$ from the algebra | $\langle0\rvert[a_k,a^\dagger_{k'}]\lvert0\rangle=(2\pi)^3\delta^3$; at coincidence $(2\pi)^3\delta^3(0)=V$ | ✅ |
| **D1 volume cancellation** (author priority 3) | $a^{\rm box}_{\mathbf k}\to V^{-1/2}a_{\mathbf k}\Rightarrow\lvert\mathbf k\rangle_{\rm box}\to V^{-1/2}\lvert\mathbf k\rangle$; projector $\to V^{-1}$, $\sum_{\mathbf k}\to V\!\int\frac{d^3k}{(2\pi)^3}$; product $=\int\frac{d^3k}{(2\pi)^3}\lvert\mathbf k\rangle\langle\mathbf k\rvert$. Cross-checked against the limit rules themselves: $\delta_{\mathbf{kk}'}\to\frac{(2\pi)^3}{V}\delta^3$ is exactly what makes $[a^{\rm box},a^{\rm box\dagger}]=\delta_{\mathbf{kk}'}$ consistent with $V^{-1}(2\pi)^3\delta^3$ | ✅ **the two factors of $V$ are exactly inverse** |
| $\mathbb{1}_1\lvert\mathbf k'\rangle=\lvert\mathbf k'\rangle$ | delta's $(2\pi)^3$ eats the measure's $(2\pi)^{-3}$ | ✅ |
| Packet inner product, smeared operators | $\langle f\lvert g\rangle=\int\frac{d^3k}{(2\pi)^3}f^*g$; $[a(f),a^\dagger(g)]=\langle f\lvert g\rangle$, a number | ✅ |
| **D3 Weyl sequence** (author priority 4) | Verified it *is* one, all three clauses: $\langle f_\sigma\lvert f_\sigma\rangle=\frac{(2\pi)^{3/2}}{\sigma^3}\cdot\frac{(2\pi\sigma^2)^{3/2}}{(2\pi)^3}=1$ **exactly** with $N=(2\pi)^{3/4}\sigma^{-3/2}$ ✅; $f_\sigma$ Schwartz, so in $\mathcal H_1$ and in $\Phi$ ✅; $\lVert(\hat{\mathbf P}-\mathbf k_0)f_\sigma\rVert^2=\langle\mathbf q^2\rangle=3\sigma^2$ since $\lvert f_\sigma\rvert^2/(2\pi)^3$ is a normalized Gaussian of variance $\sigma^2$ per component ⟹ $\sqrt3\,\sigma\to0$ ✅ | ✅ genuine Weyl sequence |
| No eigenvectors | $(\mathbf k-\mathbf k_0)f=0$ a.e. ⟹ $f=0$ in $L^2$ — uses only atomlessness of the measure | ✅ |
| **D4 unboundedness** | $\langle\mathbf k_0\lvert f_\sigma\rangle=f_\sigma(\mathbf k_0)=N=(2\pi)^{3/4}\sigma^{-3/2}\to\infty$ at $\lVert f_\sigma\rVert=1$ ⟹ Riesz bound fails | ✅ |
| Position profile and width | $\psi_\sigma=\frac{N(4\pi\sigma^2)^{3/2}}{(2\pi)^3}e^{i\mathbf k_0\mathbf x}e^{-\sigma^2\mathbf x^2}$; $\int d^3x\lvert\psi_\sigma\rvert^2=1$ verified by hand; $\Delta x=1/2\sigma$, $\Delta x\Delta P_z=\tfrac12$ exactly | ✅ |
| $\lvert\psi_\sigma(0)\rvert$ | $(2\pi)^{-9/4}(4\pi)^{3/2}\sigma^{3/2}=0.7128\,\sigma^{3/2}$ | ✅ $0.713$ |
| **$\lVert\varphi(x)\lvert0\rangle\rVert^2$** (author priority 7) | $\int\frac{d^3k}{(2\pi)^32\omega_k}=\frac{1}{4\pi^2}\int_0^\Lambda\frac{k^2dk}{\sqrt{k^2+m^2}}=\frac{1}{8\pi^2}[\Lambda\sqrt{\Lambda^2+m^2}-m^2\mathrm{arcsinh}(\Lambda/m)]$; at $\Lambda=1$ GeV, $m=135$ MeV: $\frac{1.00907\times10^6-4.929\times10^4}{78.957}=1.216\times10^4\ \mathrm{MeV}^2$ | ✅ $1.22\times10^4$ |
| — leading estimate and gap | $\Lambda^2/8\pi^2=1.267\times10^4$; ratio $0.960$ ⟹ "four percent" | ✅ |
| — conversion | $1.216\times10^4\times(197.327)^{-2}\ \mathrm{fm}^{-2}=0.312$ | ✅ $0.31\ \mathrm{fm}^{-2}$ |
| All other node-4 numbers | $V=6.89^3=327.1$ fm³ ✅ · $2\pi/L=179.9$ MeV ✅ · $\omega_0=\sqrt{135^2+180^2}=225$ exactly ✅ · $L=68.9$ fm ⟹ $18.0$ MeV ✅ · $\tfrac43\pi(50)^3/(18.0)^3=89.8\approx90$ modes ✅ · $\Delta x=1.973$ fm vs $1/m=1.462$ fm ✅ · $\sigma/k_0=27.8\%$ ✅ · $\Delta P=\sqrt3\sigma=86.6$ ✅ · $\Delta\omega=(k_0/\omega_0)\sigma=0.8\times50=40$ MeV ✅ · all five rows of the Step-5 table ($N$, $\lvert\psi_\sigma(0)\rvert$, $\Delta x$, overlap) recomputed independently ✅ · $3.27\times10^5$ fm³ and $10^{45}$ fm³ ✅ | ✅ |
| Dimensions | $[a_{\mathbf k}]=-\tfrac32$ ⟹ $[\langle\mathbf k\lvert\mathbf k\rangle]=-3$ (a volume) ✅; $[f]=-\tfrac32$ for a normalized packet ✅; $[N]=-\tfrac32$ ✅; $[\lVert\varphi\lvert0\rangle\rVert^2]=2$ ✅ | ✅ |
| Structural Stage | spectrum = support of the spectral measure, eigenvalue = an atom; Lebesgue on $[0,1]$ as the atomless example; hydrogen row $\{-13.6\,\mathrm{eV}/n^2\}\cup[0,\infty)$; direct sum → direct integral with $d\mu=d^3k/(2\pi)^3$ | ✅ (→ N-4.2 on "the box modes") |
| Mostly Faded I | $E_n=-\mu\alpha^2/2n^2$ ✅; $\delta(E-E')$ for scattering states ✅; sum-plus-integral resolution ✅; incompleteness of the bound states ✅ | ✅ |
| Mostly Faded II | line: $\sigma=[0,\infty)$, $\langle k\lvert k'\rangle=2\pi\delta$ in this branch's 1D convention ✅; interval: $\{(n\pi/L)^2\}$, $\sqrt{2/L}\sin(n\pi x/L)$ ✅; half-line: deficiency indices $(1,1)$, Robin family $\psi'(0)=c\psi(0)$, some with a bound state ✅ | ✅ |
| Transfer problem | $\hat A\ge0$ ⟹ $\sigma(\hat A)=[0,\infty)$, no bound states ✅; QNM boundary conditions with $\mathrm{Im}\,\omega<0$ diverge at **both** ends ✅; $M=60\times4.93\ \mu\mathrm{s}=2.958\times10^{-4}$ s ⟹ $f=\mathrm{Re}\,\omega/2\pi M=201$ Hz ✅ and $\tau=M/0.0890=3.3$ ms ✅ | ✅ (→ N-4.3) |
| `fill_in_formula` | `sqrt(3)*s` matches $\lVert(\hat{\mathbf P}-\mathbf k_0)f_\sigma\rVert=\sqrt3\sigma$ | ✅ |

### Node 5
| Object | Independent result | |
|---|---|---|
| **D2 on-shell Jacobian** (author priority 1) | $\frac{d\tilde k^3}{dk^3}=\gamma\big(1-\beta\frac{\partial E}{\partial k^3}\big)=\gamma\big(1-\beta\frac{k^3}{E}\big)=\frac{\gamma(E-\beta k^3)}{E}=\frac{\tilde E}{E}$, using $\partial E/\partial k^3=k^3/E$ | ✅ |
| **the $\delta^3$ transformation law** | Demanding $\int d^3k\,\delta^3 g=g$ frame-by-frame with $d^3\tilde k=(\tilde E/E)d^3k$ forces $\delta^3(\tilde{\mathbf k}-\tilde{\mathbf k}')=\frac{E}{\tilde E}\delta^3(\mathbf k-\mathbf k')$, hence $\tilde E\delta^3(\tilde{\ })=E\delta^3(\ )$ and $2E\delta^3$ invariant | ✅ |
| Boost table, all three rows exact | $\beta=0.6$: $\tilde k^3=1.25\times45=56.25$, $\tilde E=1.25\times117=146.25$, $146.25^2-56.25^2=21389.0625-3164.0625=18225=135^2$ **exactly** ✅; Jacobian $1.25\times0.52=0.65=\tilde E/E$ ✅. $\beta=0.8=k^3/E$: $\gamma=5/3$, $\tilde k^3=0$, $\tilde E=135=m$ ✅, Jacobian $=(5/3)(0.36)=0.6=1/\gamma=\tilde E/E$ ✅ | ✅ every entry |
| **D1** | $\lvert g'(\pm E)\rvert=2E$, $\theta$ deletes the negative root ⟹ $\int\frac{d^3k}{2E}$; invariance audit: $\lvert\det\Lambda\rvert=1$, scalar-of-a-scalar, $\theta(k^0)$ on $SO^+(1,3)$ only via $\Lambda^0{}_0\ge1$ and $\lvert\vec\Lambda^0\rvert=\sqrt{(\Lambda^0{}_0)^2-1}$ with $\lvert\mathbf k\rvert\le k^0$ | ✅ argument sound |
| Dimensions | $[d^3k/2E]=2=[d^4k]+[\delta(k^2-m^2)]=4-2$ | ✅ |
| **$\lvert S\rvert^{2}C=(2\pi)^{3}2E$** (author priority 2) | $\langle\mathbf k\lvert\mathbf k'\rangle=\lvert S\rvert^2C\delta^3$, invariant iff $\lvert S\rvert^2C\propto2E$ by D2 | ✅ |
| — **against staged node 2, all three rows** | Peskin $P^2C\omega=\frac{(2\pi)^3E}{(2\pi)^62E}=\frac{1}{2(2\pi)^3}$ ✅ · symmetric $\frac{E}{(2\pi)^32E}=\frac{1}{2(2\pi)^3}$ ✅ · Srednicki $\frac{(2\pi)^32E\cdot E}{(2\pi)^64E^2}=\frac{1}{2(2\pi)^3}$ ✅. And $\lvert S\rvert^2C=(2\pi)^32E$ in all three ✅ | ✅ **both identities hold in every row** |
| — the "one free choice" claim | Two independent relations on $(P,C,S)$: fix $P$ ⟹ $C$ from node 2 ⟹ $\lvert S\rvert$ from here. Exactly one free function remains | ✅ claim correct |
| — $M$ convention-independent | $M\lvert S\rvert^2C=1\Rightarrow M=\frac{1}{(2\pi)^32E}$ regardless of $S,C$ separately | ✅ |
| D3 both tests | $\langle\mathbf k\lvert\mathbf k'\rangle_R=(2\pi)^32E\delta^3$ ✅; $\langle0\rvert\varphi(x)\lvert\mathbf k\rangle=e^{-ikx}/\sqrt{2E}$ and $=e^{-ikx}$ with $\sqrt{2E}$ ✅; $\mathbb 1_1$ check cancels exactly ✅ | ✅ |
| Prefactor numbers | $1/\sqrt{450}=0.0471$, $1/\sqrt{292.5}=0.0585$, $1/\sqrt{270}=0.0609\ \mathrm{MeV}^{-1/2}$ | ✅ |
| Mismatch cost | $(90\ \mathrm{GeV})^4=6.561\times10^7\ \mathrm{GeV}^4$ | ✅ value; **the dimensionful reading of it → F-5.2** |
| **Two-body phase space, all six steps** (author priority 5) | $\delta^3$ sets $\mathbf p_2=-\mathbf p_1$; $2\pi$ count $(2\pi)^{-6+4}=(2\pi)^{-2}$, so the Step-2 blank is $4E_1E_2$ ✅; $\lvert g'(p)\rvert=p(\frac{1}{E_1}+\frac{1}{E_2})=\frac{p\sqrt s}{E_1E_2}$ ✅; **the $E_1E_2$ from the Jacobian does cancel the $E_1E_2$ from the two measures** ✅; collecting, $\frac{p^*d\Omega}{16\pi^2\sqrt s}$, and the boxed $\frac{1}{8\pi}\frac{2p^*}{\sqrt s}\frac{d\Omega}{4\pi}=\frac{p^*d\Omega}{16\pi^2\sqrt s}$ — identical | ✅ |
| — checks | $[d\Pi_2]=0$, $[d\Pi_n]=2n-4$ ✅; massless $p^*=\sqrt s/2\Rightarrow\int d\Pi_2=1/8\pi$ ✅ | ✅ |
| **$\Gamma$** | $p^*=\frac m2\sqrt{1-4\mu^2/m^2}$ ⟹ $\int d\Pi_2=\frac{1}{8\pi}\sqrt{1-4\mu^2/m^2}$ ⟹ $\Gamma=\frac{\lvert\mathcal M\rvert^2}{16\pi m}\sqrt{1-4\mu^2/m^2}$; the $1/2m$ is the inverse norm of the decaying state — this node's convention, as the hint says | ✅ (→ N-5.3) |
| $\sigma$ and node 24 | $F=2s$ massless ⟹ $\sigma=\lvert\mathcal M\rvert^2/16\pi s$ ✅; matching $4\pi\alpha^2/3s$ ⟹ $\lvert\mathcal M\rvert^2=64\pi^2\alpha^2/3$, dimensionless ✅ | ✅ |
| Structural Stage | $H^+_m\cong SO^+(1,3)/SO(3)$ ✅; uniqueness of the invariant measure on a transitive orbit ✅; massless: orbit is the forward cone minus its vertex, stabilizer $ISO(2)$ ✅. **The conceded fact checked**: induced metric $h_{ij}=\delta_{ij}-k_ik_j/E^2$, $\det h=m^2/E^2$, $\sqrt{\det h}\,d^3k=\frac mE d^3k=2m\cdot\frac{d^3k}{2E}$ — agrees up to the constant $2m$, exactly as claimed | ✅ |
| **Transfer problem, $410\ \mathrm{cm}^{-3}$ recomputed from scratch** (author priority 6) | $n_\gamma=\frac{2}{(2\pi)^3}4\pi T^3\!\int\!\frac{x^2dx}{e^x-1}=\frac{2\zeta(3)}{\pi^2}T^3$ ✅. $T=2.725\times8.6173\times10^{-5}=2.3482\times10^{-4}$ eV ✅; $\times5.0677\times10^4\ \mathrm{cm^{-1}eV^{-1}}=11.900\ \mathrm{cm^{-1}}$; $T^3=1684.9$; $\times0.24359=\mathbf{410.4\ cm^{-3}}$ | ✅ |
| — energy density and ratio | $\rho_\gamma=\frac{\pi^2}{15}T^4$ ✅; $\rho/n=\frac{\pi^4}{30\zeta(3)}T=2.701\,T$ ✅ | ✅ |
| — the four-vector/tensor argument | $d^3x\,d^3p$ invariant + $d^3p/E$ invariant ⟹ $f$ scalar and $\int\frac{d^3p}{E}p^\mu f$ manifestly a four-vector; $N^0$ and $T^{00}$ reduce correctly to (c)'s and (d)'s integrals with the $1/E$ cancelling the $p^0$. **The claim that the measure had to carry the $1/E$ is exactly right** | ✅ |
| **Cross-node $\Delta$ (author priority 8b)** | Node 5's $i\Delta(z)=\int\frac{d^3k}{(2\pi)^32\omega}(e^{-ikz}-e^{+ikz})$ ⟹ $\Delta=-\int\frac{d^3k}{(2\pi)^3\omega}\sin(kz)$, character-for-character node 2's staged phase-2 line 213 | ✅ no fork |
| **Cross-node "both claim node 2 forces their row" (8c)** | Node 4's claim is about $M$ (the completeness measure, forced by $C$ through $\mathbb 1_1\lvert\mathbf k'\rangle=\lvert\mathbf k'\rangle$); node 5's is about $\lvert S\rvert^2C$ (forced by covariance). **Different slots, same method, no conflict** | ✅ |
| `fill_in_formula` | `1/(8*pi)` matches the massless $\int d\Pi_2$ | ✅ |

---

## 3. Compliance checks

### Convention consistency, staged 1 → 2 → 3 → drafts 4 → 5
- Signature `(+,-,-,-)`: greppable in both `node.yaml` files and in every phase file carrying physics (0,1,2,3,6 on both), matching the nodes 2–3 pattern exactly. ✅
- Same $e^{-ikx}$ on the annihilation operator, same $1/\sqrt{2\omega_{\mathbf k}}$ inside the expansion, same $(2\pi)^3$ with every $d^3k$ and in the ladder commutator, same $\pi=\dot\varphi$. ✅
- **Node 4 adds exactly one row** (the one-particle resolution of the identity) and correctly marks it *not independent* — fixed by node 2's commutator. It leaves the state-normalization row blank and says so in four places. ✅
- **Node 5 fills node 1's blank row** with $\lvert\mathbf k\rangle_R=\sqrt{2E_{\mathbf k}}a^\dagger_{\mathbf k}\lvert0\rangle$ and closes the table. After F-5.1 this is now stated consistently in all four places it appears (`node.yaml` header, phase-0 Wonder Hook, phase-2 Bridging + Derivation, phase-3 Full Example). ✅
- The retrofit is honest: node 4's results are statements about $a_{\mathbf k}$, $\varphi$ and $\lvert0\rangle$, and nothing derived there is invalidated. ✅ (N-5.4)

### Tier-C
- Rating-3 row on both nodes: "Phase 2 is read **at speed** and Phase 3 is done **from the Mostly Faded Example down**. Neither is skipped." **No rating skips phases 2–3.** ✅
- Tier-C declaration paragraph present exactly once on each, in M11c's corrected post-review wording ("the lowest of the assessment's three physics blocks", Block C mean 0.85, C1 non-fluent, expertise reversal as a boundary condition about *correct* prior knowledge). Node 4 carries the extra non-generic paragraph the map's E2 on-ramp requires ("substrate and gap are the same object one step apart; item 1(a) at 3 licenses nothing here"). ✅
- `TIER-C: relaxation OFF (Gate 6 D-G6b)` greppable in both `node.yaml` files and both `phase-0.md` banners. ✅
- Ordering rule (phases 4–6 strict at every score) present on both. ✅
- No `node.yaml` field beyond spec v1.2 on either node; F4 respected, `deny_unknown_fields` not provoked. ✅
- F5: no misconception typed `inversion`; all eight types drawn from the schema enum, and both headers restate the module-wide convention. ✅

### Correctness gate / routing
- **Node 4**: correctness gate on probe item 2(b) — *any* reason for $\lvert x\rangle\notin\mathcal H$ other than non-normalizability ⟹ Phase 2 mandatory, before Phase 3, in order from the Concrete Stage, overriding a page of 3s. Matches M10a §4 node 4 exactly, including the map's requirement that the gate not fire on non-normalizability itself. The rationale is written out and is a *good* one (the correct answer is reusable; the dimension-counting answer will recur in node 5 where indices genuinely exist). ✅
- **Node 4's E12 flag** is correctly a **report, not a route**: a 0 on item 1(a) is a premise signal (the clean-productions list losing a member), recorded, flagged to the module log, orchestrator decides, node taken anyway. Matches M10a §6's note that E12 "belongs in the vault record before it belongs in the node count". ✅
- **Node 5**: no correctness gate, per the map, and the absence is stated as deliberate in `node.yaml`, the phase-0 banner and routing rule 2. What replaces it is the map's stronger clause — **phases 2 and 3 both taken at any score** — with the map's own reason (designated convention-table node; skipping produces silent factor errors five nodes later) written out and priced. Matches M10a §4 node 5 exactly. ✅ *(F-5.2's fix leaves this rationale intact: it rests on there being no single wrong probe answer, not on the error being undetectable.)*
- Escalation: node 5's `node.yaml` and Recall Prompt both flag that the M10a §6 decision is taken immediately after this node and that condition (b) is unevaluable without the per-phase time log. ✅
- **No rating skips phases 2–3 anywhere on either node.** ✅

### Measured fidelity — the priority check
Every `[MEASURED]` claim on both nodes was checked against `wiki/meta/qg-knowledge-state.md` (and, for node 5, `wiki/sources/ba-thesis-qcd-sidis-2022.md`, `wiki/analyses/qg-knowledge-baseline.md`, `wiki/analyses/qg-curriculum.md`) **verbatim**.

- **Node 4** — "rubric 1, self 2, the assessment's largest self-overrating, jointly with E1" ✅ (*"The largest self-overrating of the assessment (Δ1 on both E1 and E2)"*). "A fluent genuine production" ✅ verbatim. Spectrum of $\hat x$ answered *"we use the energy-eigenstates as spectrum?"* ✅ verbatim, and typed at grading as the spectrum ↔ eigenbasis-of-a-different-operator conflation ✅. *"wrong register"* rather than wrong content ✅ verbatim. "$\lvert x\rangle\notin\mathcal H$ because it is 4D" ✅ (N-4.4 on the expansion). Block C mean 0.85 ✅, Block E mean 0.56 ✅, C1 non-fluent with "named, never constructed / no mode expansion, no propagator, no $i\epsilon$" ✅ verbatim. "Domain concept at true zero" and the B2 yellow flag hardening to firm ✅ verbatim. **One drift found: F-4.2.**
- **Node 5** — the BA-thesis on-ramp is the strongest-attested claim on either node. *"hand-derived 2→3 phase space, **mass-dependent traces**"* is attested in three independent vault pages, and `ba-thesis-qcd-sidis-2022.md` records "Full 2→3 Lorentz-invariant phase space (dLips) derived from scratch … a substantial multi-page original derivation". The node's "(confirmed at full ingest)" is exactly right and the "invariant" qualifier is map-sanctioned (M10a §4 node 5). ✅ C4's *"used $\alpha_s(\mu)$ daily in the BA thesis → used, not understood"* ✅ verbatim. The three-firing convention-trap ledger (freedom↔flatness, Fourier↔Legendre, μ↔Λ_QCD) is cited as a **class**, not as a verbatim item, and says so ✅. The five-firing geometry attractor is enumerated correctly (A5 · C5 written · node-probe item 6 · node-probe item 3 · C5 oral lapse) ✅. **No drift found.**
- Neither node claims a `[MEASURED]` misconception the map does not place there. Node 4 carries §5 rows 8 and 9 as MEASURED and its other two as standard; node 5 carries a MEASURED *class* and a MEASURED *attractor* and says which is which. ✅

### Two-basin distractor rule
All 12 `multiple_choice` items audited individually. **Every one carries ≥1 geometry-basin and ≥1 pQCD-basin distractor**, all plausible-at-a-glance.

- **Node 4** (6 items) — geometry: metric-induced momentum-space volume element · coordinate chart fixes the spectrum · $\sqrt{-g}$ in $\mathbb 1_1$ · $\sqrt{-g}$ makes both divergences finite · indefinite metric from the spacetime signature · scalar density of weight $\tfrac12$ omitting $\sqrt{-g}$. pQCD: $\sqrt Z$ per external leg (the hardest lure on the node) · IR/UV cutoff scales as "the spectrum" · colour index $\delta^{cd}$ with a $2\omega_{\mathbf k}$ · dim reg in $d=4-\epsilon$ with one counterterm · $\mu$-dependent subtraction making $\hat H$ self-adjoint · colour-singlet composite contracted with $T^a$.
- **Node 5** (6 items) — geometry: $2E$ as $\sqrt{-g}$ on the mass shell · $\sqrt{-g}d^3k$ makes the delta a scalar · $\sqrt{-g}$ in the state · $\sqrt{-g}$ per leg as a conversion · $2\pi$'s from $\sqrt{-g}d^3p$ · omitted $\sqrt{-g}$ giving coordinate dependence. pQCD: RG-invariance of $E/\mu$ · factorization scale and the soft region · $\sqrt Z$ as the asymptotic-state normalization · agreement only at $\mu=\omega_{\mathbf k}$ with running between · colour-singlet projection imposing conservation · counterterms removing the spurious divergence.
- The map's **mandated** $\sqrt{-g}$ geometry distractor for node 5 is present in **all six** items (N-5.1), and is additionally refuted at length in the Structural Stage — including the honest concession that the induced hyperboloid volume element *does* agree up to a constant, which I verified. pQCD lures are near-misses one symbol away, as the map requires for a module where the basin is native. ✅

### Spec / structure
- `./target/debug/validate` exit 0 on both, at draft and at staged paths. ✅
- `run_mechanical_checks()` (imported and called; the module has no `__main__`): all PASS on both staged nodes. Node 5's `prerequisite_existence` failed at draft path with exactly the predicted message and resolved on staging node 4 first — **M11b's and M11c's staging-order diagnosis confirmed for the third time**. ✅
- 7 phases, per-phase `estimated_minutes` summing to the declared 150 on both (validator check 14). ✅
- `### Assumptions` present in both Derivation blocks (6 on each). ✅
- `fill_in_formula`: one per node, both scalar and index-free (`sqrt(3)*s`, `1/(8*pi)`), both physically correct, per spec §6's tensor-grading prohibition, both carrying the renderer platform note. ✅

### **Probe-range ruling (protocol item 6) — the spec range is REFERENCE-ONLY**
Ruled from the code and the spec text, not from the map:

1. **The validator has no item-count check.** Check 15 requires `calibration_probe` in Phase 0 `requires` at graduate tier; check 8 requires the matching `## Calibration Probe` H2. `crates/domain/src/content_spec.rs` contains nothing that parses probe items. Confirmed by running the validator: exit 0 on both nodes, at 3 items and at 2.
2. **The spec says so explicitly.** §6's "Not validated (deliberate)" paragraph: *"the granularity rule (§1), the novel-element budget, the time bands, and **the content of the calibration probe** are authoring judgment, enforced by review rather than by `validate_node()`."*
3. **The 4–6 figure is introduced as an exemplar**, not a rule: *"The M1b pilot node's **form is the reference**: 4–6 items covering the declared prerequisites, a 0–3 self-rating scale, and a routing table."* The binding clause in the same paragraph is *"A probe must state, for each item, what the result means"* — which both nodes satisfy for every item.

**Ruling: reference-only. No fix, no deviation.** M10a §4 specifies two items for node 5 and three for node 4; the map governs, and both probes cover every declared prerequisite and route every outcome. Recorded as N-4.5 / N-5.2 rather than as a finding. *(A reviewer applying the range as a hard gate would have to fail node 4 as well, and node 1 — the branch entry node — has four; the range has never been enforced on this branch.)*

### Author's judgment calls — rulings

| Call | Ruling |
|---|---|
| `depth_tier: trunk` + `node_type: concept` on both | **ACCEPT.** Node 4 hard-gates 5, 6 and 12; node 5 hard-gates 6, 8, 9, 12, 16 and 23 and is re-opened by name at 23. Neither is a terminal elaboration; both declare the schema default explicitly, which is the right call for a field a reviewer will check. Same ruling M11c gave nodes 2–3, on the same reasoning. |
| `structural_stage` on **both** | **ACCEPT, and it is load-bearing on both.** Node 4's — spectrum as the support of a spectral measure, eigenbasis as its atomic part, with hydrogen's mixed spectrum refuting both structural misconceptions at once — is the sharpest available treatment of the declared `conflation` and is genuinely a statement with no physics in it. Node 5's — the mass shell as $SO^+(1,3)/SO(3)$ with an essentially unique invariant measure — is the place the map's **mandated** geometry-basin `conflation` is refuted at the level it is actually held, and it is the honest forward link to node 7. Declaring it on two consecutive nodes is not inflation: it is where the two nodes' misconceptions actually live. Both spec-legal; the block is optional and unenforced. |
| `bloom_minimum: analyze` on both | **ACCEPT both, change neither, and escalate.** M11c's N-3.2 recorded that nodes 1, 2 and 3 use the field as the *characteristic level of the node's central claim* rather than as a literal floor, and that the convention is nowhere written down. M11b declared node 4 by the **floor** reading ("its floor is genuinely analytic") and node 5 by the floor reading too, explicitly declining to raise it ("node 5's evaluative content sits above the floor rather than at it"). Under the floor reading `analyze` is right for both. Under M11c's characteristic reading, node 5's central claim — "a convention is only wrong when it is mixed", which its own comment grades `evaluate` — would take `evaluate`, as node 3 did. **So the branch now demonstrably contains one node declared each way, which is the concrete inconsistency M11c predicted.** Changing either value would be picking the convention, which is Gate 8's call and not this review's; the values are left and the collision is escalated with a worked example. See §5. |
| Node 5's two-item probe | **ACCEPT** — see the probe-range ruling above. |

### Word cap
| Node | Before | After | Cap |
|---|---|---|---|
| 4 | 14 997 | **14 998** | 15 000 ✅ |
| 5 | 13 532 | **13 807** | 15 000 ✅ |

Node 4 arrived with **three words of headroom**, so F-4.1 was made affordable inside node 4's own files: N-4.1's disambiguation saved three words, F-4.2's narrowing saved one, and one redundant clause in the same paragraph as F-4.1 was trimmed ("The resolution is not that one side is approximate. It is that" → "The resolution is not approximation:"). **Nothing a correction needed was cut, and no file outside the one carrying the defect was touched to pay for it.** This is the second consecutive review in which the cap has been the binding constraint on a fix; see §5.

---

## 4. Staging

Staged in dependency order (node 5's prerequisites include node 4), one commit each, on `mission/M11-s05-nodes-2-5`:

1. `ef86212` `docs(M11d):` — the two node-4 and three node-5 draft fixes.
2. `content(M11d):` — node 4 → `content/quantum-field-theory/hilbert-space-for-fields-and-continuum-normalization/`.
3. `content(M11d):` — node 5 → `content/quantum-field-theory/lorentz-invariant-measure-and-normalization-conventions/`.

`diff -r` confirms each staged directory is byte-identical to its (fixed) draft.

**Post-staging verification**
- `./target/debug/validate` on both staged paths: **exit 0**. ✅
- `run_mechanical_checks()` on both staged paths: **all PASS** — node 4 *"All 2 internal prerequisites exist; 1 external (exempt): dirac-notation-and-hilbert-space"*, node 5 *"All 3 internal prerequisites exist; 1 external (exempt): special-relativity-four-vectors"*. ✅
- `cargo test --workspace`: **green** — 214 passed, 0 failed, 6 ignored across 8 suites. Per M11c, the `--features ssr` compile failure is pre-existing and content-independent; not chased, and the default suite is what was confirmed. ✅

Ingest **not** run (law 7 — the orchestrator's act, post-merge). HEAD left on `mission/M11-s05-nodes-2-5`. Nothing pushed. The main checkout and `pt-M12` were never touched.

**Module S0.5 status: nodes 1–5 all staged in `content/quantum-field-theory/`.** The M10a §6 escalation decision is now due.

---

## 5. For Gate 8

1. **The `bloom_minimum` collision M11c predicted has now happened, with a worked example.** Node 3 was declared `evaluate` under the "characteristic level of the central claim" reading; node 5 was declared `analyze` under the literal-floor reading *while its own comment grades its central claim `evaluate`*. Both are defensible, neither is wrong, and they cannot both be the convention. One sentence in the node map or spec §3 settles it — and until it is settled, every subsequent node inherits a coin flip. Not this review's to decide; both values left as authored.
2. **The word cap has now bound two reviews in a row, and this time it bound a *correction*.** Node 4 arrived with three words of headroom, so fixing a genuine physics error required finding three words of prose to delete in the same file. The fix was clean here — a redundant clause existed — but that is luck, not design. M11c raised this; the second data point is that a node can arrive at the cap with a defect in it, which means the cap is currently a constraint on **correctness**, not only on length. Recommend a ruling: hard gate with a stated review tolerance (e.g. reviews may exceed by ≤1% with the overrun logged), or an explicit instruction that authors ship at ≤14 800.
3. **F-4.2 confirms F-3.4 is a *repeating* failure mode, and narrows where it lives.** Two of three authored batches have now put an unattested embellishment into `[MEASURED]` **on-ramp prose** specifically — never into a misconception statement, never into `node.yaml`'s cited fields. M11c's proposed fix (require on-ramp claims to carry their vault citation inline) is now supported by two independent instances and should be adopted rather than left as a suggestion. Note that node 5's on-ramp — the one place M11b's own notes flagged as needing checking — was *clean*, and clean precisely because it carries its provenance inline in `node.yaml` ("confirmed at full ingest"). The proposed fix has already been demonstrated to work.
4. **A new failure mode, not seen in M11c: the Phase-0 framing contradicting the node's own Derivation.** Both of node 5's MAJORs are of this shape — the Wonder Hook stating a taxonomy and a diagnostic claim that phases 2 and 3 then refute. Neither is detectable by re-deriving equations, which is what this gate's method is built for; both were caught only by reading the node's *claims about itself* against its own later sections. Recommend adding an explicit reviewer step: **check the Wonder Hook and the Abstract/Derivation conclusions against each other**, node by node. It is cheap and it is where the two most expensive defects of this batch were.
5. **A node's own convention decision should appear in the node map.** F-5.1 was possible because M10a §4 node 5 says the freedom to move the $\sqrt{2E}$ "is exactly the freedom in which textbooks differ" without saying which slot the branch ends up choosing. The author chose correctly in the Derivation and incorrectly in the framing, and nothing upstream could adjudicate. For the remaining 19 nodes, any node that closes a convention row should have that row's final value written into the map.
6. **Staging-order artefact, third confirmation, now fully characterised.** Node 5 failed `prerequisite_existence` at draft path on node 4 alone and passed immediately once node 4 was staged. The gate resolves internal prerequisites by `rglob` over `content/` only. This is now a known, benign, three-times-reproduced artefact; it would be worth teaching the gate to also search a `--draft-root`, or it will be re-diagnosed by every future reviewer.
7. **Node 1's Conventions table says "Nothing here is forced — every row is a choice."** Node 2 proved the ladder-commutator row is *not* free, node 4 marked its added row "Not independent", and node 5 has now proved the state-normalization row is **forced** once covariance is demanded. Node 5's reflection question 1(iii) even asks the learner to sort the table into forced and free — against a table whose header says everything is free. Node 1 is staged and outside this mission's scope, so nothing was changed; flagged because it is a one-line edit to node 1 that three later nodes are now contradicting.

---

*M11d — nodes 4 and 5 reviewed by independent re-derivation. 2 MAJORs and 3 MINORs found and fixed, 0 unresolved MAJORs, both staged. Validator exit 0, quality gate all PASS, `cargo test --workspace` green. HEAD on `mission/M11-s05-nodes-2-5`.*
