---
phase: 999.1-quiz-ux-improvements
plan: "01"
subsystem: api
tags: [xp, hints, latex, markdown, tdd, rust]

# Dependency graph
requires:
  - phase: 05-gamification-and-personal-tree
    provides: compute_xp function and XP award pipeline
  - phase: 03-content-and-simulations
    provides: markdown_renderer.rs with LaTeX extraction logic
provides:
  - compute_xp with hints_used: bool parameter (50% penalty, no perfect bonus on hints)
  - extract_latex_placeholders() public utility function
  - Refactored render_content_markdown to use shared LaTeX utility
affects:
  - 999.1-02 (quiz UI uses extract_latex_placeholders for rendering)
  - 999.1-03 (award-xp endpoint will pass req.hints_used to compute_xp)

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "TDD: write failing tests first, implement to pass, then refactor"
    - "extract_latex_placeholders gated behind cfg(not(target_arch = \"wasm32\")) — SSR-only utility"

key-files:
  created: []
  modified:
    - crates/db/src/xp_logic.rs
    - crates/app/src/components/content/markdown_renderer.rs
    - crates/server/src/handlers/progress.rs
    - crates/db/src/review_repo.rs

key-decisions:
  - "compute_xp callers progress.rs and review_repo.rs pass false for hints_used — Plan 03 will wire req.hints_used for the award-xp path"
  - "extract_latex_placeholders is cfg(not(target_arch = wasm32)) not cfg(feature = ssr) — avoids including in WASM bundle while being callable outside the ssr feature boundary"

patterns-established:
  - "Hint penalty: 50% XP multiplier applied to score-scaled base; perfect bonus suppressed when hints_used=true"
  - "LaTeX extraction shared utility pattern: extract then render — used by both render_content_markdown and future quiz field processors"

requirements-completed: [D-10, D-11, D-04]

# Metrics
duration: 4min
completed: 2026-03-25
---

# Phase 999.1 Plan 01: Quiz UX Improvements — Foundation Summary

**compute_xp extended with hints_used: bool (50% XP penalty, no perfect bonus) and extract_latex_placeholders() extracted as shared SSR utility from markdown_renderer**

## Performance

- **Duration:** ~4 min
- **Started:** 2026-03-25T09:43:57Z
- **Completed:** 2026-03-25T09:47:14Z
- **Tasks:** 2 features (TDD: RED + GREEN for each)
- **Files modified:** 4

## Accomplishments
- Added `hints_used: bool` parameter to `compute_xp` — hint usage halves XP and suppresses the 1.5x perfect bonus (D-10, D-11)
- Updated existing `compute_xp` tests to pass `false` (no regression) and added 8 new hint-penalty tests
- Updated callers in `progress.rs` and `review_repo.rs` to compile with `hints_used: false` (Plan 03 will wire `req.hints_used`)
- Extracted `extract_latex_placeholders()` as a public `#[cfg(not(target_arch = "wasm32"))]` function from the LaTeX regex logic inside `render_content_markdown`
- Refactored `render_content_markdown` to call the new utility (DRY, no behavior change)
- Added 6 TDD tests for `extract_latex_placeholders` covering inline, display, no-math, mixed, single-dollar edge case, and HTML-escaping

## Task Commits

Each feature was committed atomically (TDD: failing tests confirmed before implementation):

1. **Feature 1: compute_xp hint penalty** - `45ca041` (feat)
2. **Feature 2: extract_latex_placeholders** - `269be94` (feat)

## Files Created/Modified
- `crates/db/src/xp_logic.rs` — `compute_xp` signature updated, 8 new hint-penalty tests added, existing tests updated
- `crates/app/src/components/content/markdown_renderer.rs` — `extract_latex_placeholders` public function added, `render_content_markdown` refactored to use it, 6 new tests
- `crates/server/src/handlers/progress.rs` — caller updated to `compute_xp(..., false)`
- `crates/db/src/review_repo.rs` — caller updated to `compute_xp(..., false)`

## Decisions Made
- Callers `progress.rs` and `review_repo.rs` pass `false` for `hints_used` temporarily — Plan 03 will update `AwardXpRequest` to include `hints_used` and wire it through the endpoint.
- `extract_latex_placeholders` uses `cfg(not(target_arch = "wasm32"))` rather than `cfg(feature = "ssr")` to keep it accessible from non-SSR server contexts without dragging it into the WASM bundle.

## Deviations from Plan

None — plan executed exactly as written.

## Issues Encountered
None.

## User Setup Required
None — no external service configuration required.

## Next Phase Readiness
- Plan 02 can now call `extract_latex_placeholders` in quiz field rendering paths
- Plan 03 can update `AwardXpRequest` to add `hints_used: bool` and pass it to `compute_xp`
- All tests green, workspace compiles cleanly

---
*Phase: 999.1-quiz-ux-improvements*
*Completed: 2026-03-25*
